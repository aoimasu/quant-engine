/target
**/*.rs.bk
Cargo.lock.bak
.DS_Store

# Local env files may hold real secrets — never commit them. `.env.example` is committed.
.env
.env.local

# Frontend (web/) — Node toolchain
web/node_modules/
web/dist/
web/coverage/
web/*.local
web/.vite/
