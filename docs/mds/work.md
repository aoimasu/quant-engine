# Work — PR under review (transient scratchpad)

Transient scratchpad for the PR currently under review only. On merge the approved block is archived to
`docs/mds/reviewed/<ticket>.md` and this file is cleared back to empty. `main` is an unprotected repo, so
merges are governed by this skill's review loop: no PR merges before its entry is marked `[Approved]`, and the
green gate is the full LOCAL build/test/clippy/deny/all-features suite passing on the reviewed commit.

---

_No PR currently under review._
