# QE-466 — Vintage leaderboard/comparison (informational, NOT a selector) — Review record

- **Ticket:** QE-466 · **Branch:** `qe-466/vintage-leaderboard` · **PR:** https://github.com/aoimasu/quant-engine/pull/179
- **Merged commit (branch head):** `dd47fc0`
- **Base:** `main`
- **Evidence note:** [`docs/operations/qe-466-vintage-leaderboard-analysis.md`](../../operations/qe-466-vintage-leaderboard-analysis.md)
- **Ticket detail:** [`docs/mds/tickets/QE-466.md`](../tickets/QE-466.md)
- **Design ref:** `docs/architecture/qe-455-research-flow-design.md` §9, §3 (best-of-N REJECTED), §11.1 (dominant risk)

### Green-gate (all on commit `dd47fc0`)
Rust: `cargo fmt --all --check` PASS · `clippy --workspace --all-targets -D warnings` PASS · `cargo test --workspace` PASS (91 bins) · firewall PASS · `cargo deny check` PASS
Frontend: `npm ci && npm run lint` PASS · `npm run build` PASS · `npm test` PASS (113)

### Consultation-budget enforcement posture: (b) `own-evidence-only`
Rank only on each vintage's own already-deflated sealed evidence; NO fresh cross-vintage selection statistic on holdout verdicts. QE-430 cross-vintage correlation is a diversity DIAGNOSTIC (effective N), never fed into the rank. Posture (a) (max-statistic/SPA across the displayed set) would itself be a fresh selection statistic over holdout verdicts — the exact surface §3/§11.1 warns regrows into the outer best-of-N selector.

### Acceptance criteria — all met
- [x] Ranks sealed vintages on the PERSISTED net-of-cost figure (`SealEvidence.cost_stress_net_min`, deployed capacity-capped) / capacity-at-size / turnover / cross-vintage correlation (QE-430 R(N)/Fisher-z, effective N shown) + steer diffs; gross/equal-weight/lone-Sharpe/in-sample ABSENT.
- [x] Consultation budget ENFORCED: over-consulted vintages sort BELOW within-budget ones regardless of net + DSR bar escalated (posture (b) stated + tested).
- [x] NO seal/promote/select/auto-run endpoint or action (GET-only, POST→405; recursive no-action-key test); every vintage "not paper-confirmed"; promotion stays via the existing gate/seal (owes G2/G3).
- [x] Standing best-of-N caveat present.

### Review verdict — Approved (reviewer, commit `dd47fc0`)

Meets all AC; no blocking issues. This is the surface the program most guards against becoming a selector, and it is
built to be structurally incapable of selecting. Verified against QE-466.md AC, design §9/§3/§11.1, the reused
QE-456/457/467/430/460 pieces, the diff, and independent runs of BOTH gates: server 176/176 (leaderboard read 6/6),
firewall 1/1 (independently re-traced), web `VintageLeaderboard` 6/6.

1. **Structurally not a selector — CONFIRMED (dominant risk).** `GET /api/vintages/leaderboard` GET-only (POST→405 tested); DTO carries no promote/select/seal/auto-run field; `leaderboard_exposes_no_promote_or_select_action…` walks the ENTIRE nested response via a recursive `find_key` asserting none of `promote`/`select`/`seal`/`winner`/`auto_run`/`best`. Frontend asserts no such button (only read-only inspector navigation).
2. **No recompute — CONFIRMED.** Every ranked number READ from the sealed artefact; the QE-430 correlation is a diversity diagnostic that does NOT feed the rank.
3. **Ranking metric (`cost_stress_net_min`) — ACCEPTABLE (ruled).** The correct already-persisted net-of-cost scalar (min{1×,2×} cost-stressed net of the deployed capacity-capped ensemble); alternatives are forbidden (recompute) or wrong (DSR is a deflation stat).
4. **Consultation budget ENFORCED — CONFIRMED.** Over-consulted vintages sort below within-budget ones regardless of net (a higher-net over-consulted vintage is demoted with an escalated DSR bar, test-proven) — not a mere visual grey.
5. **Firewall edge `qe-server → qe-ensemble` — LEGAL (ruled, independently re-traced).** qe-server's forbidden set is the five live crates (qe-ensemble not among them); qe-ensemble's production closure hits none of them and does NOT reach qe-wfo in production (only a dev-dep of qe-determinism, structurally excluded) — no search⊥portfolio bridge. Firewall test passes.
6. **Effective-N shown; forbidden metrics absent; not-paper-confirmed + best-of-N caveat present; no gate/seal/schema/version change — CONFIRMED.**

**Ruling on `HOLDOUT_CONSULTATION_BUDGET = 1`: ACCEPTABLE** — a principled default (design §4: the backtest is the single recorded consultation), documented as product-liftable.

### Non-blocking notes (do not block merge)
1. The diagnostic correlation aligns series by leading-bar minimum length (no timestamps for exact alignment) — documented v1 limitation; does not affect ranking (not a rank input).
2. `cost_stress_net_min: None` sinks a vintage to the bottom of its budget group, implicitly favouring flow vintages that ran the cost-stress sweep.
