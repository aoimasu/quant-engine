# QE-448 — Correct SPA label (White's RC) + document deflation biases + universe provenance — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/150 (squash-merged)
- **Branch**: qe-448/spa-label-deflation-bias
- **Implementation commit**: cf6180c
- **Spec of record**: `docs/reviews/2026-07-16-maxdama-panel-review.md#qe-448` (panel P3/clarity)
- **Evidence note**: `docs/architecture/qe-448-spa-label-deflation-bias-design.md`

## Acceptance criteria
- [x] SPA module labelled accurately (recenters all k models = White's RC / SPA-lower, not Hansen's SPA), conservatism documented, Hansen noted as follow-up.
- [x] Deflation boundary documented (DSR/PBO/SPA correct for SELECTION, not per-trade cost/adverse-selection/survivorship); point-in-time universe provenance asserted in the lineage.
- [x] Golden-safe; green gate + firewall green.

## Implementation
Relabel (option a, golden-safe): fixed the `spa.rs` module/type/fn + crate doc from "Hansen's SPA" → "White's Reality Check / SPA-lower" (the code recenters all k models by their full mean with no `√(2 log log T)` omission gate); documented conservatism; noted Hansen SPA-consistent as a follow-up. No public rename (`reality_check_pvalue` already accurate). Added a semantics test. Universe provenance **already captured**: `Lineage::from_config` folds `Config::content_hash` (SHA-256 over the whole `Config` incl. the point-in-time `universe`) — documented + regression test. Deflation boundary documented in the `qe-validation` crate doc.

## Verification (LOCAL green gate re-run by reviewer on `cf6180c`, CI disabled — all PASS)
fmt · clippy locked + all-features `-D warnings` · `cargo test --workspace --locked` (**896 passed, 2 ignored**) · deny · firewall · spa (5) + universe_membership (1). No golden moved (regenerate → empty).

## Review verdict — [Approved] (0 blocking, 1 non-blocking)
1. **Label accuracy verified against the actual code.** `reality_check_pvalue` recenters every model by its full-sample mean (`resampled_mean − means[k]`) with **no** per-model omission gate and **no** `√(2 log log T)` threshold → genuinely White's RC (raw) / Hansen SPA-lower (studentised), NOT Hansen consistent. The old "Hansen's SPA" was the mislabel; the relabel matches the code. Conservatism + studentize clarifications correct. Hansen SPA-consistent honestly deferred (would move the computed p / churn fixtures / flip G1 — disproportionate to a P3 clarity fix).
2. **Semantics test non-vacuous.** A high-variance mean-≈0 decoy pollutes the raw null max → `p_decoy > p_base` — genuinely exhibits the all-model-recentred SPA-lower power loss.
3. **Universe provenance already in the lineage hash.** `Config::content_hash = SHA-256(serde_json::to_vec(config))` hashes the `universe` (instrument + `[listed, delisted)`); `Lineage::from_config` folds it into the id. Regression test moves a delisting date → both `config_hash` and lineage id change (non-vacuous, genuinely flows universe → hash).
4. **Deflation-boundary doc clear + correct** ("a clean DSR/PBO/SPA is evidence the *selection* was honest — never proof the *inputs* were"; survivorship corrected by the point-in-time universe QE-012 in the lineage SHA).
5. **No golden moved** (docs/comments + tests + note only, no computation change, regenerate → empty); **firewall clean** (`qe-validation → qe-determinism`).

### Non-blocking follow-ups (accepted)
1. The semantics-test comment "Hansen's consistent SPA would threshold the decoy out" is imprecise — Hansen's omission gate drops strongly-negative-mean models, not high-variance mean-0 ones; it is studentisation, not omission, that would tame the decoy. Core demonstration genuine; only the one comment is loose — reword.
2. **Hansen SPA-consistent** (model-omission recentring, `√(2 log log T)`) to recover power is a deferred follow-up (moves the p-value → needs fixture regen; out of scope for the P3 clarity fix).
