# QE-433 — Portfolio-level fractional (≤½) empirical-Kelly sizing — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/135 (squash-merged)
- **Branch**: qe-433/portfolio-kelly-sizing
- **Implementation commit**: 2f5f5ca
- **Spec of record**: `docs/reviews/2026-07-16-maxdama-panel-review.md#qe-433`
- **Evidence note**: `docs/architecture/qe-433-portfolio-kelly-sizing-design.md`

## Acceptance criteria
- [x] The sizer can cut leverage (fractional Kelly on a fat-left-tail combined series < naive summed size) and never exceeds the pretrade cap.
- [x] Two positively-correlated members are down-weighted vs summing standalone Kellys.
- [x] Per-vintage, content-addressed, reproducible; determinism harness green.

## Implementation
`crates/wfo/src/kelly.rs`: `empirical_kelly`/`fractional_kelly` = `argmax_{f≥0} mean ln(1+f·r)` via golden-section on reused `log_growth`; correlation-robust (reads realised joint path, no covariance). `crates/risk/src/sizer.rs`: `PortfolioSizer` content-addressed sidecar (κ·f* multiplier, `Default`=neutral 1.0). `crates/vintage`: hashed `VintageContent.sizer`, `VINTAGE_FORMAT_VERSION` 4→5. `train.rs` solves on the capacity-weighted net-of-(QE-431-calibrated)-cost combined series and seals the sizer. `live_netter::net_positions_sized` applies the multiplier and clamps `|net| ≤ leverage_cap`, panic-free (QE-268).

## Verification (LOCAL green gate re-run by reviewer on `2f5f5ca`, CI disabled — all PASS)
fmt · clippy locked + all-features `-D warnings` · `cargo test --workspace --locked` (**798 passed, 2 ignored**) · deny · firewall · kelly/sizer/live_netter targeted tests.

## Review verdict — [Approved] (0 blocking, 3 non-blocking)
1. **Never-exceed-cap SAFETY (CRITICAL) — confirmed for all input classes.** `apply_sizer` scales then clamps `|net| ≤ cap`: κ·f*>1 → exactly cap; short → sign preserved; cap=0 → 0; cap<0 → 0 via `.max(0)`; division guarded (`|net|>cap≥0 ⇒ |net|>0`). `multiplier()` ≥ 0 by construction (non-finite → 0, fail-safe, never sizes up on a broken solve). Panic-freedom compile-enforced (QE-268 deny lint at `live_netter.rs:22`, clippy `-D warnings` passes). `sizer_never_exceeds_pretrade_cap` drives the **real** `PreTradeGovernor`, asserting `Send(≤ cap·equity)`.
2. **Kelly math — correct.** Golden-section on `[0, hi]` (hi inside the ruin boundary); `log_growth(f·r)` strictly concave ⇒ unimodal ⇒ valid; non-positive drift → 0. Correlation-robust (positive correlation inflates combined variance, pulling f* down). κ∈[0.3,0.5] clamped. Tests non-vacuous: fat-vs-thin at same mean proves the tail (not mean) cuts f*; correlated-downweight asserts both `portfolio_corr < kelly(A)+kelly(B)` and `corr_ratio < indep_ratio` (isolates correlation from averaging).
3. **Golden-regeneration integrity — independently reproduced byte-for-byte** (`regenerate_fixtures`; regenerated `content_hash acad59dc…` matches). Field diff exactly `format_version 4→5` + new `"sizer":{"multiplier":"1"}` after `slippage`; CLI/server fixtures byte-identical; 4→5 justified.
4. **Calibrated-cost — confirmed.** f* solved on the capacity-weighted combined series priced by the QE-431 `SlippageCalibration`, not stale costs.
5. **Firewall clean; green gate re-verified on `2f5f5ca`.**

### Non-blocking follow-ups (accepted)
1. **Sealed sizer is not yet wired into a live netting loop** — `net_positions_sized` is called only from tests; `PositionNetter` has no production/runtime consumer, so the κ·f* multiplier is **inert in production today**. Safe direction (pre-QE-433 behaviour preserved, hard cap still governs), pre-disclosed in the design note's "Scope containment" and consistent with the QE-429/QE-431 live-wiring deferral pattern. **Follow-up:** thread `net_positions_sized(..., Some(cap))` into the real live netting flow when the two-process live entry is built (the caller MUST pass `Some(cap)`).
2. `PortfolioSizer::content_hash()` is currently test-only (production reproducibility rides the embedded field in the `VintageContent` hash).
3. When wired live, verify the caller always supplies the cap.
