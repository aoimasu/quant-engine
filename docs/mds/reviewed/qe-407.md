# QE-407 — Server run-lifecycle: graceful shutdown, supervised-task registry, honest success — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/112 (squash-merged)
- **Branch**: qe-407/run-lifecycle-shutdown
- **Implementation commit**: de2bc8d
- **Spec of record**: `docs/reviews/2026-07-15-team-improvement-review.md` → `### QE-407`
- **Evidence note**: `docs/architecture/qe-407-run-lifecycle-shutdown-design.md`

## Acceptance criteria
- [x] SIGTERM stops the listener, drains or terminally-marks in-flight runs (no `running` meta left for a clean shutdown), returns success; a hard-killed run is reconciled on next boot (`shutdown_drains_running_run_and_stops_accepting`, `reconcile_orphans_fails_running_leftovers_but_keeps_terminal`, non-vacuous).
- [x] A fake job that prints `done`/exits 0/writes nothing ⇒ `failed` with the reason; happy path still `succeeded` (`done_without_result_json_is_failed_not_succeeded`).

## Verification
Local + CI green: fmt/clippy(-D warnings)/test(all)/deny/firewall. Reviewer re-ran qe-server `--test runs` (10/10, incl. 3 new AC tests + happy-path) + firewall — all pass. Cargo.lock byte-identical (only additive `tokio` `signal` feature); no golden touched; no new crate edge.

## Review verdict — [Approved]
AC-1 shutdown (core) is correct and the test is non-vacuous. `main.rs` wires `axum::serve(listener, router).with_graceful_shutdown(shutdown_signal())` and, after `serve()` resolves, calls `shutdown_manager.shutdown(DEFAULT_SHUTDOWN_DRAIN=20s)`. `RunManager::shutdown` sets `accepting=false` (fresh `create` → `CreateError::ShuttingDown` → 503), closes the semaphore to wake any queued supervisor blocked on `acquire()` (drains cleanly via the existing Err arm), then bounded-drains the registry against a single fixed deadline — a naturally-finishing handle keeps its own terminal `meta.json`; a survivor is `abort()`ed, awaited to settle (Child dropped → `kill_on_drop`), and terminally marked `failed` only if still non-terminal, so no `running` meta survives a clean shutdown. The registry holds the lock across `spawn` + `insert` while the task's self-deregister takes the same lock only after `supervise` completes → insert-before-remove (no leaked live entry, no double-remove).

AC-1 reconcile: `reconcile_orphans()` is called before serving, fails every `Queued`/`Running` leftover and leaves terminal runs untouched. Running on a fresh process before any run can be created, an orphan is unambiguous — it cannot fail a legitimately-live run (correctly pairs with QE-263). AC-2 honest success: classifier is now `done && exit==0 && store.result_path(&id).exists() ⇒ Succeeded`, else `done && exit==0 ⇒ finish_failed("job reported done but wrote no result.json")`, TODO removed. The failure branch is proven (failed + honest reason + empty artifacts + `/result` 409 reporting failed, not falsely-green); happy path still `succeeded`, and the real CLI writes `result.json` before `emit_done`/`emit_train_done` (unchanged in this PR), so legitimate jobs are not regressed.

Panic-free on the lifecycle path: `shutdown_signal()` installs `ctrl_c` + unix `SIGTERM`, falling back to a never-resolving `pending()` (not a panic) if a handler can't be installed; manager paths use `if let Ok(Some(..))` / `Result` propagation / `let _ = handle.await` with no `unwrap`/`expect` (clippy `-D warnings` under `unwrap_used=deny` passed). Firewall/scope: `crates/server` gains no `qe-runtime`/`qe-venue`/`qe-cli`/`qe-wfo`/`qe-ensemble` edge (firewall green); only additive `tokio` `signal` feature (pulls only already-present `signal-hook-registry`), so `Cargo.lock` byte-identical and `deny` stays green; blast radius confined to `crates/server` (+ the workspace `Cargo.toml` feature line); no golden touched.

- nit (non-blocking): the drain is sequential per handle against a shared shrinking deadline, so a run late in iteration order gets whatever budget remains. Total drain stays bounded (≤20s) and honest — best-effort fairness only; a concurrent `join_all` under a single timeout would give every in-flight run the full window. Worth considering if run counts grow.
- nit (non-blocking): `DEFAULT_SHUTDOWN_DRAIN = 20s` is reasonable but hardcoded; if the container stop grace period is tightened below it, a wedged child could be SIGKILLed mid-terminal-write. Consider surfacing the drain window as config alongside the other server knobs. → server-config theme (QE-425 / QE-429).
