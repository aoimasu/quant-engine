# QE-442 — Graded (probability-surface) signal combination — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/144 (squash-merged)
- **Branch**: qe-442/graded-signal-combination
- **Implementation commit**: 9e8ed0f
- **Spec of record**: `docs/reviews/2026-07-16-maxdama-panel-review.md#qe-442`
- **Evidence note**: `docs/architecture/qe-442-graded-signal-combination-design.md`

## Acceptance criteria
- [x] Ordinal `QState` conviction (distance-into-band + count-of-satisfied-clauses) feeds entry strength/size; band-edge acts weaker than deep-in-band.
- [x] `Genome::decide` stays pure; **batch==streaming byte-identical** over the graded path; deterministic (exact Decimal, no machine float on the sealed path).
- [x] No committed golden moved; green gate + firewall green; train⟂live cross only through signal/domain.

## Implementation
Additive, default-off. `Genome::decide`/`Decision` **unchanged** (pure boolean k-of-n). New pure `qe-signal` methods `RuleSet::graded_conviction` + `Genome::entry_conviction`/`entry_strength` → exact `Decimal` in `[0.5, 1]` from ordinal `QState`. wfo backtester scales entry notional by it under `BacktestConfig.graded` (default false), enabled on training/selection/reporting. No `REP_VERSION`/`VINTAGE_FORMAT_VERSION` bump. `entry_strength` exposed on `qe-signal` for a future live consumer.

## Verification (LOCAL green gate re-run by reviewer on `9e8ed0f`, CI disabled — all PASS)
fmt · clippy locked + all-features `-D warnings` · `cargo test --workspace --locked` (**862 passed, 2 ignored**) · deny · firewall.

## Review verdict — [Approved] (0 blocking, 2 non-blocking)
1. **`decide` purity + batch/streaming parity over the GRADED path (CRITICAL) — confirmed non-vacuously.** `decide`/`Decision` literally unchanged; grading is a separate pure point-wise FIR/leakage-safe fn (`Σcontrib/Σcap`, no RNG/clock/rolling). The guard `graded_entry_strength_is_batch_streaming_identical` uses a **wide-band** genome and explicitly asserts `batch.iter().any(|v| entry_strength(v) < 1)` — proving the `<1` surface is exercised — then asserts `decide`/`entry_conviction`/`entry_strength` byte-identical bar-for-bar batch vs streaming.
2. **Determinism on the sealed path.** `entry_strength = floor + (1−floor)·conviction ∈ [0.5,1]` via exact `rust_decimal` integer division; wfo sizing `notional = size_frac·strength·equity_prev` all Decimal — no machine float on the sealed money path.
3. **No committed golden moved — verified.** `git diff --stat` shows no fixture/golden JSON; `regenerate_fixtures` → empty diff. The fixture genome's span-1 band `[3,4]` → cap=1, conviction=1, strength=1.0 → grading is a mathematical no-op there. `65f009d4→fca21d36` is the ephemeral `run_train_job` determinism artifact (asserted for same-seed reproducibility, not a pinned literal); real evolved wide-band genomes rescale entry *sizes* (returns/weights/DSR/equity move; **trades/decisions unchanged**).
4. **Additive / back-compat** — no serialized genome field, `graded` default-false byte-identical to pre-QE-442 (`graded_off_is_byte_identical_to_classic_sizing`); old genomes decode identically.
5. **Direction sanity real** (band-edge < deep-in-band, smooth [0.5,1] modulation).
6. **Composition** — grading applied consistently across the whole training pipeline **and** reporting (coherent: it's the strategy's genuine sizing rule, evaluated identically by selection/deflation/reporting — unlike QE-441's selection-only shocks). QE-435 parity still passes; QE-441/QE-433 compose unchanged. Firewall clean (signal+wfo only).

### Non-blocking follow-ups (accepted)
1. **Train↔live SIZING gap:** grading is on training/reporting but NOT wired into live sizing (`hedger`/`edge` have no `entry_strength`) — training/reporting size by conviction, live sizes full. Not a live regression (additive/default-off there; live sizing unchanged). `entry_strength` is exposed on `qe-signal` for a future live consumer — **wire it into live sizing** when the two-process live entry is built (same live-wiring cohort as QE-433 Kelly sizer, QE-435 cost model, QE-429 breaker); QE-435's oracle should then assert graded-sizing parity.
2. `entry_strength_exact_decimal_values` pins clean values — a repeating-conviction literal (1/3, 2/3) would make sealed-sizing determinism explicit.
