# QE-208 — mark EMA loop + tick observer — [Approved]

- **PR:** #60 — https://github.com/aoimasu/quant-engine/pull/60 (squash-merged)
- **Ticket:** QE-208 (`Phase: P2` · `Area: ④ Live pipeline` · `Depends on: QE-202`)
- **Branch:** `qe-208/mark-ema-tick-observer` (deleted post-merge)
- **Merged commit subject:** `QE-208: mark EMA loop + tick observer (#60)`
- **Reviewed commits:** `e76804e` (pass 1) → `3619c66` (pass 2, review-nit cleanup)
- **Evidence / design:** `docs/architecture/qe-208-mark-ema-tick-observer-design.md`
- **Changed files:** `crates/runtime/src/live_mark.rs` (new), `crates/runtime/src/lib.rs`, design note.

### Goal
Slow-DD probing rides a smoothed mark (EMA τ½=60s); the tick observer feeds the breaker layer. EMA loop
on markPrice@1s; both smoothed and raw ticks available to breakers.

### Acceptance criteria (from backlog)
- [x] EMA half-life is correct — `ema_half_life_is_correct` (step→~50 after one half-life; tolerance `<0.05`).
- [x] Both smoothed and raw mark ticks are available to breakers — `both_raw_and_smoothed_reach_the_observer`.

### Implementation summary
- `live_mark` module: `MarkTick { event_time_ms, raw, smoothed }`; `MarkEmaLoop` (wraps QE-116
  `qe_risk::MarkEma`; `spec_baseline()` = τ½=60s/1s, `observe`, `drive`, `smoothed`); `MarkTickObserver`
  trait + blanket impl for `FnMut(&MarkTick)`.
- Raw exposed alongside smoothed so a fast/raw tier (QE-116/D1 A3 alternative, not baseline) needs no
  second pipeline. Breaker logic out of scope (QE-212). Operates on already-decoded marks (per QE-205).

### Verification (toolchain 1.96.0)
- `cargo fmt --all --check` — clean · `cargo clippy --workspace --all-targets --locked -- -D warnings` — clean
- `cargo test --workspace --locked` — 56 suites ok, 0 failed (qe-runtime: 30 tests)
- `cargo-deny check` — advisories/bans/licenses/sources ok · CI on PR #60: 4/4 passed, CLEAN.

### Review notes — [Approved] (two passes)

**Pass 1 (@ `e76804e`).** No blocking defects. Verified:
- AC1 (half-life) genuinely proven against the **real** `qe_risk::MarkEma` (not a re-impl): `v_60 = 100·(1 −
  (0.5^(1/60))^60) = 50` exactly, so the `≈50` assertion validates τ½=60s. Seeding correct (first sample seeds
  `value = price`).
- AC2 (both raw + smoothed to breakers): every `MarkTick` carries raw + smoothed; `drive()` forwards each to
  the `MarkTickObserver`; test confirms raw==input, seed smoothed==raw, and smoothed lags on a moving series.
- Loop mechanics (seed, order/event-time preservation, `smoothed()` accessor) tested.
- `MarkTickObserver` + `FnMut` blanket impl sound as the QE-212 seam (no coherence conflict).
- Scope: deferring wss decode consistent with QE-205; not building the raw-mark fast tier correct (QE-116/D1
  A3 = documented alternative, not baseline). Float only in alpha (inherited from QE-116) + test tolerance.

Non-blocking notes: (1) half-life tolerance could be tighter; (2) `drive()` dual output deserves a doc line;
(3) loop trusts input ordering (acceptable per QE-205 contract).

**Pass 2 (@ `3619c66`, delta `e76804e..3619c66`) — [Approved] confirmed.** Test/doc-only follow-up:
- #1 tolerance tightened `<1.0` → `<0.05` (true error ~1e-9 from alpha's f64→Decimal rounding over 60 Decimal
  steps; robust, now a real regression guard on the alpha formula). Comment accurate.
- #2 `drive()` doc paragraph added (returned Vec and observer receive the same ticks — convenience batch, not
  a second dispatch). Accurate.
- #3 left as-is, deferred to runtime plumbing per QE-205. No dep change → green gate on `3619c66` still valid.
