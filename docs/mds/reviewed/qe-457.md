# QE-457 — SPA Vintage Inspector screen (mirrors evolve PoolReview) — Review record

- **Ticket:** QE-457 · **Branch:** `qe-457/vintage-inspector-spa` · **PR:** https://github.com/aoimasu/quant-engine/pull/170
- **Merged commit (branch head):** `312945e`
- **Base:** `main`
- **Design/evidence note:** [`docs/architecture/qe-457-vintage-inspector-design.md`](../../architecture/qe-457-vintage-inspector-design.md)
- **Ticket detail:** [`docs/mds/tickets/QE-457.md`](../tickets/QE-457.md)
- **Design ref:** `docs/architecture/qe-455-research-flow-design.md` §7.2, §4.1; QE-450 §13.5

### Green-gate (all on commit `312945e`)
Frontend (web/ touched): `npm ci` PASS · `npm run lint` PASS · `npm run build` PASS · `npm test` PASS (84 tests / 22 files)
Rust (insurance): `cargo fmt --all --check` PASS · `clippy -D warnings` PASS · `cargo test --workspace` PASS · firewall PASS · `cargo deny check` PASS

### Route / components
Route: `strategies` nav renders `StrategiesArea` (was placeholder at `App.tsx:121`). Files:
`web/src/app/strategies/StrategiesArea.tsx`, `VintageBrowser.tsx`, `VintageInspector.tsx` (+ `ProvenanceBanner`),
`VintageInspector.test.tsx`, `web/src/api/runs.ts` (`getVintage(id)` + `VintageDetail` DTOs), `web/src/app/App.tsx`.

### Acceptance criteria — all met
- [x] Screen renders provenance banner, composition (chromosome→indicators→weight), selected indicators, full
  gate/deflation evidence, holdout split + regime composition from QE-456's payload; placeholder removed.
- [x] Synthetic/mixed vintage shows unmistakable provenance banner; `mixed` never shown as `real` (all 3 states tested).
- [x] Gate card leads with net-of-cost/tradability + honest demoted deflation basis; no standalone health badge;
  carries "not paper-confirmed" label.
- [x] No promote/seal/select control anywhere (test-asserted).
- [x] Component tests cover composition table, gate card, provenance banner (real/synthetic/mixed), mirroring PoolReview.test.tsx.

### Review verdict — Approved (reviewer, commit `312945e`)

Meets all AC; no blocking issues. Verified against QE-457.md AC, design §7.2/§4.1, QE-450 §13.5, the `PoolReview`
reference, the diff, and an independent test run (`npx vitest run VintageInspector.test.tsx` → 9/9 green).

**AC verification (UI-honesty ticket)**
1. **Provenance banner** — `ProvenanceBanner` keys copy+CSS off `data_provenance`; synthetic="Synthetic data — NOT REAL", mixed="Mixed real + synthetic / NOT a pure real-data vintage" (loud `--warn-500`), real muted. Three tests assert real/synthetic/mixed; mixed never renders "Real market data". PASS.
2. **Gate-card hierarchy (QE-450 §13.5)** — `GateEvidence` renders "Net-of-cost / tradability (leads)" first (cost-stress min{1×,2×}, realised turnover, capacity_usd, consultations), then a demoted "Deflation basis (honest — no lone health tile)" block (DSR "necessary — not sufficient", uncensored PBO, PBO/CSCV, distinct-trial N "vs E[maxSharpe] bar", SPA, IC/FDR). No standalone health badge. PASS.
3. **No client-side recompute** — `num`/`usd` are pure presentation formatters; every number verbatim from the QE-456 payload. PASS.
4. **Inspection-only** — no seal/promote/select/approve/revoke control; test asserts every button is the /all vintages/i back-nav and no /seal|promote|select|approve|revoke|reject/i button exists. PASS.
5. **"Backtest-holdout only — not paper-confirmed"** standing `Callout`, always rendered; test covers it. PASS.
6. **Holdout panel** shows `{train_range, embargo_bars, holdout_range}` + series length AND regime composition (labels+bars+share); test asserts both ranges + all three regime labels. PASS.
7. **Placeholder removed**; tests mirror `PoolReview.test.tsx`; TS DTOs match QE-456 server field names exactly. PASS.

**Non-blocking notes / follow-ups (not required for merge):**
1. **FOLLOW-UP (QE-467 schema gap, not a QE-457 defect):** QE-450 §13.5 mandates "uncensored PBO WITH its population size", but QE-467's `SealEvidence` persists `n_trials` (DSR distinct-trial N) and NOT `variance_trials` (the Sharpe-dispersion PBO population). QE-456 can't expose it → QE-457 labels the slot but has no number. Suggest a follow-up adding `variance_trials` to QE-467's `SealEvidence` + the QE-456 payload so this screen fully honours the §13.5 stat set. (On the train path `uncensored_pbo` itself is `None`, so both show "—" for train vintages.)
2. **FOLLOW-UP:** `GET /api/vintages` list summary omits `data_provenance`, so provenance shows on the inspector detail only, not the browse table. Would need a list-enrichment ticket.
3. Holdout regime share `%` is benign client-side share-of-total display arithmetic, not gate recomputation. Noted for completeness only.
