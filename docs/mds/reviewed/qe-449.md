# QE-449 — Guard the unused maker rate against adverse-selection blindness — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/151 (squash-merged)
- **Branch**: qe-449/maker-rate-guard
- **Implementation commit**: 47094a2 (fix pass; round-1 was cfcfe1d)
- **Spec of record**: `docs/reviews/2026-07-16-maxdama-panel-review.md#qe-449` (panel P3/latent — the last maxdama recommendation)
- **Evidence note**: `docs/architecture/qe-449-maker-rate-guard-design.md`

## Acceptance criteria
- [x] Document on `FeeSchedule` that the maker rate must NOT fill orders without a paired fill-conditional adverse-selection markout (§7.6).
- [x] A guard test asserts the **production** backtest fill path (`apply_fill`) only ever charges the taker rate.
- [x] Behaviour-preserving (pure-taker today); no golden moved; green gate + firewall green.

## Implementation
**Pure-taker confirmed** (reviewer-swept edge/hedger/wfo/venue/runtime/risk): `apply_fill` (`backtest.rs:380`) hardcodes `Liquidity::Taker`; every `Liquidity::Maker` is enum-def/`rate()`-arm/test/doc; zero `post_only`/`OrderType`/limit machinery. **Doc invariant** on `FeeSchedule`/`Liquidity::Maker`: the maker rate must not fill without a paired adverse-selection markout (a resting maker fill is systematically selected against; collecting spread/rebate without the markout overstates PnL and inflates a Sharpe DSR can't deflate), pointing at QE-444 `alpha_loss` as the home for such a term.

## Review — two rounds
**Round 1 (cfcfe1d): [Approved], 1 nit.** Pure-taker premise + doc invariant confirmed correct, but the guard test exercised `friction::simulate` (no production caller), NOT the production `apply_fill` it claimed to guard → a maker routing in `apply_fill` would pass silently; AC2 not literally met. Orchestrator sent it back to guard the real path.

**Round 2 (47094a2): [Approved], 0 blocking, AC2 met.**
- **New production-path guard** `apply_fill_charges_the_taker_rate_not_the_maker_rate`: drives a real `backtest()` → `backtest_with_trades → apply_fill` (constant price → `trades==1` = 2 fills; slippage/funding zeroed so `net_pnl` is pure fee drag). Asserts `net_pnl == −2·taker·size_frac` exactly, `!= −2·maker` (non-vacuous), AND the **load-bearing** 10×-maker-bump-leaves-net_pnl-byte-identical check. A future `Liquidity::Maker` routing into `apply_fill` breaks both the maker-bump equality and the exact taker-drag assertion → the test fails, forcing the author to add the §7.6 markout.
- Old `simulate` test renamed `simulate_over_taker_fills_charges_the_taker_rate_only` (accurate "dev/test helper" wording); over-claiming comments corrected; `FeeSchedule` doc points at the production-path guard.
- Golden-safe (diff = `backtest.rs` + `friction.rs` + note; regenerate → empty); firewall clean.

## Verification (LOCAL green gate re-run by reviewer on `47094a2`, CI disabled — all PASS)
fmt · clippy locked + all-features `-D warnings` · `cargo test --workspace --locked` (**898 passed, 2 ignored**; new guard runs) · deny · firewall.

### Non-blocking follow-ups (accepted)
- Hansen SPA-consistent (from QE-448) and the live adverse-selection markout wiring (if/when `post_only` maker fills are ever added, pair them with the QE-444 `alpha_loss` fill-conditional markout) remain future work — the guard ensures a maker-fill change cannot land silently.
