# QE-251 — `qe-cli backtest` job (run a vintage over a window) — [Approved]

- **PR:** #79 — https://github.com/aoimasu/quant-engine/pull/79 (squash-merged as `99b00bf`)
- **Ticket:** QE-251 (`Phase: PreP3` · `Area: runnable jobs / cli` · `Depends on: QE-129, QE-120, QE-107, QE-108, QE-105`)
- **Branch:** `qe-251/cli-backtest-job` (deleted post-merge)
- **Merged commit subject:** `QE-251: qe-cli backtest job (run a vintage over a window)`
- **Reviewed commits:** `bd09b37` (pass 1, [Approved] 0 blocking / 4 nits) → `37a8404` (pass 2, 3 nits resolved, approval stands)
- **Evidence / design:** `docs/architecture/qe-251-cli-backtest-job-design.md`
- **Changed files:** `crates/cli/src/jobs/{mod,result,metrics,features,backtest,datetime}.rs`,
  `crates/cli/src/lib.rs`, `crates/cli/src/main.rs`, `crates/cli/tests/backtest_job.rs`,
  `crates/cli/tests/fixtures/{sample_store/data.mdb, sample_vintage.json, golden_result.json}`,
  `docs/architecture/qe-251-cli-backtest-job-design.md` (new). Branch also carried the QE-252 archive
  bookkeeping (`docs/mds/reviewed/qe-252.md`, cleared `work.md`) in first commit `9d8c93f`.

### Goal
Make a backtest of a sealed vintage runnable from `qe-cli`, emitting JSON-line progress and a deterministic
§8.1 `result.json` — the foundation the `qe-server` and SPA are built against (decision D1: backtest a sealed
vintage, not hand-typed parameters).

### Acceptance criteria (from backlog)
- [x] A backtest over a committed fixture vintage + sample store produces a deterministic `result.json`
  matching a golden file; the progress stream ends with `{"t":"done"}`.

### Implementation summary (plan Tasks 1,2,3,5; Task 4 reused from merged QE-252)
- `Command::Backtest` parsing (defaults `taker_fee_bps=2.0`, `slippage_model="square-root-impact"`).
- §8.1 result-contract structs (`result.rs`), field names verbatim (pinned by a JSON-keys test).
- Pure metrics (`metrics.rs`): equity/dd/cagr/sharpe/sortino/monthly + **gross** `win_rate`/`profit_factor`
  (documented cost-blind, computed from `TradeFill.return_frac`; `INFINITY` no-loss convention).
- OHLCV→decision-bar feature bridge (`features.rs`) via `qe_signal::feature::assemble_batch`;
  `run_backtest` orchestration (`backtest.rs`) — load+`verify()` vintage, build decision bars,
  `backtest_with_trades` per chromosome, weight-aggregate by `weights`, `TradeFill`→`TradeRow`, metrics,
  assemble doc; progress load/scan/features/simulate/report→done.
- `main.rs` streams progress as JSON lines, writes `result.json` into `--run-dir`, exit code per outcome.
- Committed fixtures + byte-exact golden test (`backtest_over_fixture_store_matches_golden`).

### Verification (final commit `37a8404`)
- `cargo fmt --all --check` — PASS
- `cargo clippy --workspace --all-targets --locked -- -D warnings` — PASS (0 warnings)
- `cargo test --workspace --locked` — PASS (608 passed, 2 ignored)
- `cargo test -p qe-architecture --test firewall --locked` — PASS
- `cargo deny check` — PASS

### Review — [Approved] (2 passes; 0 blocking)
- **Catalogue-schema deviation traced SOUND:** the only non-test `CatalogueConfig` is `features.rs`
  `default()` (states=5); the WFO evolution engine takes a `FeatureSchema` at runtime and runs only under
  `#[cfg(test)]` — there is no production evolve path yet (`run_train` writes only a manifest). So `default()`
  is provably the only catalogue any vintage could have been evolved against; `is_valid`/`check_schema` is a
  correct backstop. Pass-1 nit that the risk note overstated the guard was fixed in pass 2.
- 1e-10 metric rounding legitimately neutralises `cagr` `powf` last-ULP cross-arch variance (not masking a
  bug); cost mapping correct; single-instrument v1 acceptable; result contract/progress/weight-aggregation/
  TradeFill mapping/golden meaningfulness/firewall all verified.
- Pass-2 nit-fixes (doc/comment-only, golden byte-identical): schema-drift note scoped to out-of-range drift;
  `slippage_model` marked nominal; universe single-instrument comment. Thousands-separators nit deferred to UI.

### RECOMMENDED FOLLOW-UP (tracked, not a QE-251 blocker)
Persist `CATALOGUE_VERSION` (+ `states`) in `VintageContent` and assert on load, converting silent catalogue
identity/ordering/version drift into a loud failure. Correct today (only the default catalogue exists), but a
latent trap once QE-260 wires real training with potentially different catalogue configs. **Candidate new
backlog ticket for the orchestrator's final report.**
