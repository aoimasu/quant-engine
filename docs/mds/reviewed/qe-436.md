# QE-436 — In-search parsimony (MDL) tie-break, wired at the graduation gate — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/138 (squash-merged)
- **Branch**: qe-436/parsimony-mdl
- **Implementation commit**: ddaee27 (fix pass; round-1 was 2f30ecb)
- **Spec of record**: `docs/reviews/2026-07-16-maxdama-panel-review.md#qe-436`
- **Evidence note**: `docs/architecture/qe-436-parsimony-mdl-design.md`

## Acceptance criteria
- [x] A parsimony/MDL cost enters selection as a tie-break (equal robust fitness → fewer clauses wins), **operationalised in-search** — `train.rs` maps every ensemble-selected member through `archive.parsimonious_equal` before sealing `chromosomes`.
- [x] The MDL λ stays OUT of the per-genome fitness that feeds DSR (no deflation-basis distortion).
- [x] Deterministic; green gate + firewall green; golden unmoved.

## Implementation
`Genome::mdl_complexity()` (enabled clauses + distinct referenced features — pure deterministic proxy). `fitness.rs`: `within_noise_band` + `should_replace_parsimonious` (fitness-first, complexity only inside the k_sigma band); `should_replace` left pure. `lifecycle.rs`: `robust_lower_bound`/`graduation_cmp`/`most_parsimonious`. **Wired (fix pass):** `MapElitesArchive::parsimonious_equal(genome, gate)` (`mapelites.rs`) returns the most parsimonious among niche elites tied on stored scalar fitness; `train.rs` maps every ensemble-selected member through it before sealing `chromosomes`.

## Review — two rounds
**Round 1 (2f30ecb): [Approved] correctness/safety, 0 blocking, but flagged PARTIAL** — the tie-break machinery was correct/DSR-safe/tested but had **zero production callers** (dormant), so the running search behaved identically to `main`; rank-#7 "operationalise in-search" was not achieved. Orchestrator sent it back to wire it.

**Round 2 (ddaee27): [Approved], 0 blocking, AC1 now met.**
1. **Now genuinely LIVE.** `train.rs:404–407` maps every ensemble-selected member through `archive.parsimonious_equal(...)` on the seal path. New test `parsimonious_equal_deploys_the_simpler_of_two_tied_niche_elites` is non-vacuous (two equal-fitness elites, complex inserted first → simpler deployed; higher-fitness elite never swapped; un-archived genome unchanged).
2. **DSR-safety holds after wiring (traced).** `n_trials = effective_trials(occupied_cells)`, `variance_returns = cell_champion_returns(archive)` (QE-414 per-niche champions), and the `pool` trial columns (`elite_pool` computed at line 374, **before** the swap at 404) all derive from the **unchanged** archive. Only `in_sample_returns` (the deployed candidate) depends on `chromosomes`. `parsimonious_equal` takes `&self` (no mutation), reads only `e.fitness`+`e.genome`+`mdl_complexity`. Deploying a different member changes neither the DSR variance population nor the trial count. MDL never enters V, N, `pool`, or any fitness value.
3. **Tie on the right key.** Filters niche elites to exact stored-fitness equality (`e.fitness == fitness`, never `.returns`), `std_error=0.0` collapses the band to exact equality → a material fitness difference is never swapped (proven by the sole-holder-at-2.0 test). `most_parsimonious` deterministic (earliest on tie).
4. **Golden unmoved.** No fixture/golden/Cargo change; no `VINTAGE_FORMAT_VERSION` bump; `train_job` (determinism assertion) + backtest golden pass unchanged. "Unmoved on the fixture" is behaviour-preservation (no tie fires among the fixture's selected members), not a global no-op — the live test proves general activity.
5. **Overstatement corrected** in the evidence note + PR body.

## Verification (LOCAL green gate re-run by reviewer on `ddaee27`, CI disabled — all PASS)
fmt · clippy locked + all-features `-D warnings` · `cargo test --workspace --locked` (**823 passed, 2 ignored**) · deny · firewall · live-wiring test + both golden guards.

### Non-blocking follow-ups (accepted)
1. The "wiring is live" test guards the `parsimonious_equal` function, not the `train.rs` call site (no tie fires on the fixture, so deleting the mapping wouldn't fail a test) — a tiny train-path fixture with a genuine tie would regression-guard the call site.
2. **Second half deferred:** two-stage size/rule decoupling or an alpha-quality term would put a term into the DSR-facing scalar fitness (forbidden) and exceeds Effort:S — documented follow-up (naturally handled inside the GP program, QE-451).
