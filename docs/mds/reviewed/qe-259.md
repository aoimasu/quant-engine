# QE-259 — Backtest screens wired to the API — [Approved]

- **PR:** #86 — https://github.com/aoimasu/quant-engine/pull/86 (squash-merged as `8f035af`)
- **Ticket:** QE-259 (`Phase: PreP3` · `Area: frontend` · `Depends on: QE-255, QE-257, QE-258`)
- **Branch:** `qe-259/backtest-screens-wired` (deleted post-merge)
- **Merged commit subject:** `QE-259: backtest screens wired to the API`
- **Reviewed commits:** `fe83f4b` (pass 1, [Approved] 0 blocking / 4 findings) → `2728e07` (pass 2, findings 3&4 resolved, approval stands)
- **Evidence / design:** `docs/architecture/qe-259-backtest-screens-design.md`
- **Changed files:** `web/src/` (four screens + `BacktestsArea` + `api/runs.ts` + primitives Input/Select/DataTable/
  Tag/Pnl/Tabs + Icon), `web/src/**/*.test.tsx`, `crates/server/src/auth/mod.rs` + `crates/server/tests/auth.rs`
  (rejection redirect), design note. Branch carried QE-258 archive bookkeeping in `11d22d8`.
- **Note:** completed across a resumed session — the impl agent hit a session limit mid-task and resumed on the
  same branch from its uncommitted state (bookkeeping already committed).

### Goal
The core user journey: trigger a backtest, watch progress, review results.

### Acceptance criteria (from backlog)
- [x] A backtest can be triggered from the UI, its progress polled to completion, and the full result contract
  rendered (metrics strip, equity/drawdown, monthly heatmap, trades table).

### Implementation summary
- Four wired screens: Backtests list (`GET /api/runs`), New-backtest form (`POST /api/runs`, vintage/window/
  resolution/universe/costs, client validation + inline 400s), Backtest result (DesignSync port of
  `BacktestResearch.jsx`; §8.1-driven from `GET /api/runs/:id/result`; **resilient** progress-card polling of
  `GET /api/runs/:id` while running; 6-metric strip; equity/drawdown inline-SVG charts; monthly heatmap; Trades
  table; **read-only** genome params + Config; **Re-run** clones params → new POST), Market-data coverage
  (`GET /api/market-data/coverage`). `BacktestsArea` view-state machine (no router dep).
- DesignSync-ported primitives (Input/Select/DataTable/Tag/Pnl/Tabs). CSP-safe hand-rolled SVG charts + CSS-grid
  heatmap (no chart lib, no runtime CDN; built `dist/` scanned clean). No new npm deps.
- **Completed the QE-258 rejection flow:** `crates/server/src/auth/mod.rs` allowlist-reject now `302 →
  /?error=forbidden` (fixed local Location, no open-redirect) instead of JSON 403; auth test updated; 401/200
  paths unchanged; all auth tests green. This is exactly the `?error=forbidden` the SPA's `detectRejection` reads.

### Verification — BOTH gates PASS (final commit `2728e07`; reviewer independently rebuilt)
- Rust: `fmt`/`clippy -D warnings`/`test --workspace` (664 passed, 2 ignored)/`firewall`/`deny` — PASS.
- Frontend: `npm run build` OK (CSP-safe); `npm run lint` clean; `npm test` — 8 files / **34 tests** PASS.

### Review — [Approved] (2 passes; 0 blocking; reviewer built & ran the frontend both passes)
- AC end-to-end genuinely proven (real setTimeout-chain polling with a running→succeeded phase flip, full §8.1
  render asserted, read-only params, Re-run clones). Rejection redirect correct + completes the QE-258 flow.
- Pass-2: finding 3 (polling resilience — bounded `MAX_POLL_FAILURES=4`, non-fatal retry note, streak resets on
  a clean tick; also fixed a latent bug — `resultFetched` now set only after a successful result fetch, so a
  succeeded-run-with-failing-result-endpoint retries to the cap instead of getting stuck) + finding 4 (universe
  hint names the alpha-first selected symbol = `universe[0]` the engine actually backtests).

### DEFERRED FOLLOW-UPS (non-blocking; need API-shape changes → candidate tickets)
1. Expose a **per-vintage symbol roster** on `GET /api/vintages` (currently the New-backtest universe options are
   sourced from `GET /api/market-data/coverage`; reviewer confirmed not a wrong-backtest risk since vintage
   genomes are instrument-agnostic, but a roster is cleaner).
2. Surface a **metrics summary in `RunMeta`** (or the run index) so the Backtests list can show a key-metrics
   column (spec §7.2) — metrics currently live only in `result.json`.
