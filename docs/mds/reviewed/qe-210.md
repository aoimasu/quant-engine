# QE-210 — reconstructed state — [Approved]

- **PR:** #61 — https://github.com/aoimasu/quant-engine/pull/61 (squash-merged)
- **Ticket:** QE-210 (`Phase: P2` · `Area: ③ Bootstrap` · `Depends on: QE-209`)
- **Branch:** `qe-210/reconstructed-state` (deleted post-merge)
- **Merged commit subject:** `QE-210: reconstructed state (positions, dormancy, true committed peak) (#61)`
- **Reviewed commits:** `ab66542` (pass 1) → `a21254a` (pass 2, review-nit cleanup)
- **Evidence / design:** `docs/architecture/qe-210-reconstructed-state-design.md`
- **Changed files:** `crates/runtime/src/boot_state.rs` (new), `crates/runtime/src/evaluator.rs`
  (`positions()` accessor), `crates/runtime/src/lib.rs`, design note.

### Goal
Bootstrap output: per-strategy positions, dormancy latches, and committed peak equity — load-bearing for
drawdown breakers; the peak must be the **true** committed peak, not a windowed peak.

### Acceptance criteria (from backlog)
- [x] Reconstructed committed peak equals the true historical peak on a fixture longer than the bootstrap
  window — `committed_peak_is_true_all_time_not_windowed`.

### Implementation summary
- `CommittedPeak` — true all-time running max of an equity path (never a window); the breaker's drawdown
  anchor. `DormancyLatch` — dormant iff never entered a position during replay. `StrategyState` /
  `ReconstructedState`; `from_replay(positions, decisions, equity_paths)`.
- Equity series is a declared input (net-of-cost live equity + breaker feed = QE-212/QE-217).

### Verification (toolchain 1.96.0)
- fmt/clippy clean · `cargo test --workspace --locked` — 56 suites ok (qe-runtime: 36) · deny ok ·
  CI on PR #61: 4/4 passed, CLEAN.

### Review notes — [Approved] (two passes)

**Pass 1 (@ `ab66542`).** No blocking defects. Verified:
- AC genuinely proven + non-vacuous: length-10 path peaks early at 150 then declines; `from_series` → 150
  while `trailing_window_max(series,4)` → 110 < 150. `CommittedPeak` has no windowing path at all. Matches the
  QE-116 breaker's total-drawdown anchor exactly (`breaker.rs` `p.max(equity)`; only the fast tier is
  windowed) — seeding the anchor from `committed_peak_equity` is semantically exact.
- `from_replay` correct + defensive: dormancy by scanning the trace for any `Enter` (out-of-range-safe);
  `MismatchedEquityPaths` guard (no panic/silent truncation); empty path → `None`; positions from the accessor.
- Dormancy "never entered ⇒ dormant" is a defensible cold-start rule; separates dormancy from flatness;
  breaker-driven latching deferred to QE-212.
- No firewall/parity concern: reads (not recomputes) the shared decide/advance output within qe-runtime.

**Scope judgment (critically scrutinised): the equity-path boundary is LEGITIMATE, not under-delivery.** The
AC is about peak *correctness* (fully proven); a faithful equity curve is net-of-cost (QE-109) against real
fills owned by QE-217/QE-212, which sit *after* QE-210 in the dependency graph — producing it here would
invert dependencies or duplicate the QE-120 backtester and risk train/live drift. 2 of 3 outputs (positions,
dormancy) are wired to real replay data; only the equity *series* is a declared input (same seam discipline as
QE-202/205/208).

> **FORWARD OBLIGATION (recorded for QE-211/QE-217/QE-220):** the equity series fed to `from_replay` must be
> the *authoritative net-of-cost* path, and a restart-parity/integration test (natural home **QE-220**) must
> later prove the reconstructed **equity path itself** — not just the running-max fold — matches a live
> reference. Today's tests prove the fold only.

**Pass 2 (@ `a21254a`, delta `ab66542..a21254a`) — [Approved] confirmed.** `boot_state.rs` + design note:
- #3 `debug_assert!(cd.index < positions.len(), ...)` asserts exactly the bound `get_mut` relies on; fires
  only on a genuinely out-of-range index; does not trip on existing tests; release behaviour byte-for-byte
  identical (debug_assert compiles out; get_mut is the release bounds check). Pure test-time hardening.
- #2 design-note `from_replay` signature updated to the shipped decomposed form.
- #1 forward obligation recorded (above). No dep change → green gate on `a21254a` still valid.
