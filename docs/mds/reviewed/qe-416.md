# QE-416 — Seal capacity weights + worst_case_loss + per-strategy calibration — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/108 (squash-merged)
- **Branch**: qe-416/seal-capacity-weights-calibration
- **Implementation commit**: 1ced872
- **Spec of record**: `docs/reviews/2026-07-15-team-improvement-review.md` → `### QE-416`
- **Evidence note**: `docs/architecture/qe-416-capacity-weights-calibration-seal-design.md`

## Acceptance criteria
- [x] Sealed `weights` differ from equal-weight when capacity binds (AC(a) non-vacuous across binding/non-binding/all-uneconomic branches).
- [x] `worst_case_loss` is `Some(..)` after seal (QE-130 stress set, finite non-negative).
- [x] `BreakerLayer::from_calibration` finds every sealed strategy — no pre-gating (AC(c) end-to-end via real seal → load → from_calibration).
- [x] Determinism preserved; content-hash stable; no golden touched.

## Verification
Local + CI green: fmt/clippy/test(698)/deny/firewall. Reviewer re-ran cli/ensemble/risk/runtime suites — all pass. Seal→`VintageRepository::load` (hash-verify + QE-402 assert_schema) confirms the newly-populated fields reload with a matching hash.

## Review verdict — [Approved]
Seal replaces the `1/k` / `None` / constant placeholders with QE-128 capacity-capped weights, a QE-130 `worst_case_loss` (`Some`), and a QE-116 per-strategy `CalibrationProfile` keyed by positional ids `"0".."k-1"` that exactly match `VintageContent::strategy_ids()` and the `from_calibration` lookup — so no calibrated member is fail-safe pre-gated (AC(c) proves it end-to-end and non-vacuously). Content-hash determinism holds: the idempotency fixes (`round_dp(12).normalize()` on calibration Decimals, round-to-1e12 on sealed f64 weights/worst_case_loss) are correct and empirically confirmed (real seal→load recomputes a matching hash; `quantized_thresholds_round_trip_through_serde` + `train_is_deterministic_for_a_fixed_seed` pass; no bad QE-402 interaction). AC(a) exercises all three weight branches (k=1 fixture `[1.0]` is a genuine no-bind). No fixture/golden in the diff; backtest golden still matches. Modelling constants reasonable for a first cut.

Non-blocking follow-ups (finding (1) → **tracked as QE-429**; others recorded):
1. `from_calibration` has no production caller yet — the runtime evaluator exposes `calibration()` but doesn't construct a live `BreakerLayer` from it, so the sealed thresholds + no-pre-gate guarantee are LATENT until the runtime wires `from_calibration` using `content.strategy_ids()`. → QE-429.
2. Modelling constants (TARGET_AUM $1M, quantiles 0.75/0.95/0.95, margin 1.5, gross/turnover proxies) are hardcoded and should become per-run config → folded into QE-429.
3. Minor: the "serde float parser isn't correctly-rounded" rationale is overstated (the round-to-1e12 fix is still correct); AC(b) could additionally assert `worst_case_loss > 0`.
