# QE-001 — Cargo workspace & crate topology — Approved review record

- **PR:** [#1](https://github.com/aoimasu/quant-engine/pull/1) (squash-merged → `main` as 2b977f6)
- **Branch:** `qe-001/cargo-workspace` (deleted)
- **Final commit:** e606e76
- **Evidence:** `docs/architecture/qe-001-cargo-workspace-topology-design.md`

## Acceptance criteria (met)
- [x] `cargo build --workspace` and `cargo test --workspace` succeed on a clean checkout.
- [x] A dependency check proves `runtime` does not depend on `wfo`/`ensemble` (and vice-versa);
      shared code only via `signal`/`domain`.

## Verification (independently re-run by reviewer)
- `cargo fmt --all --check` clean; `cargo build --workspace` ok; `cargo test --workspace` 9 passed;
  `cargo clippy --workspace --all-targets -- -D warnings` clean.
- Topology guard red-proofed three ways across rounds: direct `runtime→wfo` FAILS, transitive
  `venue→wfo` FAILS, and (after the round-2 `kind` filter) a `[dev-dependencies]` `runtime→wfo` PASSES
  (dev edges correctly ignored). All reverted to green.

## Review outcome
**Approved** (round 2). High-quality, appropriately-scoped scaffolding matching `docs/specs.md`
decoupling. Both round-1 non-blocking advisories resolved:
1. Topology guard filters to normal deps (`kind == null`) so dev/build deps aren't counted as
   architectural edges.
2. Design note rewritten to describe the shipped `--no-deps` member-local closure approach.
