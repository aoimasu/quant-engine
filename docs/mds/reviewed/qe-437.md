# QE-437 — Gate G1 on the already-computed PBO — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/139 (squash-merged)
- **Branch**: qe-437/g1-pbo-gate
- **Implementation commit**: f57c1f6
- **Spec of record**: `docs/reviews/2026-07-16-maxdama-panel-review.md#qe-437`
- **Evidence note**: `docs/architecture/qe-437-g1-pbo-gate-design.md`

## Acceptance criteria
- [x] A pre-registered `pbo < 0.5` criterion added to `G1Criteria`/`evaluate_g1`; `pbo ≥ 0.5` not promoted, with per-criterion evidence.
- [x] A missing/non-finite pbo fails closed (blocks), never passes vacuously.
- [x] Zero new math (uses the already-computed `RobustnessReport.pbo`); green gate + firewall green; no golden moved.

## Implementation
`crates/gate/src/lib.rs`: `DEFAULT_MAX_PBO = 0.5` (frozen const), `max_pbo` field, new `pbo_below_overfit_threshold` criterion (`pbo.is_finite() && pbo < max_pbo`) in `evaluate_g1`. `crates/cli/tests/train_job.rs`: criteria count 5→6. `G1Decision`/`G1Criteria` ride the `TrainResultDoc` sidecar, not `VintageContent` → no golden moved, `VINTAGE_FORMAT_VERSION` stays 5.

## Verification (LOCAL green gate re-run by reviewer on `f57c1f6`, CI disabled — all PASS)
fmt · clippy locked + all-features `-D warnings` · `cargo test --workspace --locked` (**825 passed, 2 ignored**) · deny · firewall · qe-gate (7).

## Review verdict — [Approved] (0 blocking, 1 doc nit)
1. **Criterion wired + fail-closed.** `pbo_below_overfit_threshold` is a real `CriterionResult`; `promoted = results.iter().all(|c| c.passed)` → it genuinely gates. Predicate `robustness.pbo.is_finite() && robustness.pbo < max_pbo` → non-finite/absent PBO short-circuits to fail ⇒ blocked (fail-closed, mirrors the holdout-sufficiency discipline). Verified by `a_non_finite_pbo_fails_closed` (NaN and +∞ both block).
2. **Pre-registered:** `DEFAULT_MAX_PBO = 0.5` frozen const ("not tuned to any fixture").
3. **Zero new math:** reads the QE-131 CSCV `RobustnessReport.pbo`, no re-derivation.
4. **Tests non-vacuous:** pbo 0.60 → `!promoted` with `failed_criteria() == ["pbo_below_overfit_threshold"]` (isolated sole blocker); pbo 0.75 → blocked with evidence; non-finite → blocked; positive path exercised (`good_robustness` pbo 0.10 now clears the new criterion in the promotion tests).
5. **No golden moved** (diff = gate lib + train_job test + design note; sidecar not hashed); firewall clean.

### Non-blocking follow-up (accepted)
1. Module doc says "five pre-registered acceptance criteria" while `evaluate_g1` emits six `CriterionResult`s (internally consistent with the file's convention — "five" counts evidence criteria + holdout-sufficiency as a precondition) — reword to "five holdout evidence criteria plus the holdout-sufficiency precondition" on the next touch of `gate/lib.rs`.
