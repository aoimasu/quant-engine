# QE-434 — Per-indicator IC screening (rank-IC + two-fold sign-consistency + BH-FDR) — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/136 (squash-merged)
- **Branch**: qe-434/ic-screening
- **Implementation commit**: d6f8982
- **Spec of record**: `docs/reviews/2026-07-16-maxdama-panel-review.md#qe-434`
- **Evidence note**: `docs/architecture/qe-434-ic-screening-design.md`
- **Implementation**: `crates/validation/src/ic.rs` (+ `lib.rs` exports)

## Acceptance criteria
- [x] Rank-IC + IC-by-horizon per indicator, out-of-fold; zero-IC factors dropped/flagged.
- [x] Admit only on second-fold same-sign comparable-magnitude IC AND a Benjamini-Hochberg FDR pass across all screened indicators.
- [x] Screen filters compute not hypothesis count; tests non-vacuous; green gate + firewall green.

## Implementation
`qe-validation::ic` — **report-only + opt-in** (enforced admission deferred; no vintage/golden moves). Pure numeric over f64 signal columns + net-return series + fold index sets → `Admit/Flag/Drop`. Home is `qe-validation` (depends only on `qe-determinism`). Rank-IC (Spearman), IC-by-horizon, two-fold sign-consistency, BH-FDR.

## Verification (LOCAL green gate re-run by reviewer on `d6f8982`, CI disabled — all PASS)
fmt · clippy locked + all-features `-D warnings` · `cargo test --workspace --locked` (**811 passed, 2 ignored**) · deny · firewall · `qe-validation ic` (15).

## Review verdict — [Approved] (0 blocking, 3 non-blocking)
1. **BH-FDR correct.** Step-up: sort p ascending, largest k with `p_(k) ≤ (k/m)·q`, reject the k smallest (realigned to input order). Hand-verified `p=[0.001,0.02,0.049,0.5], q=0.05 → k_max=2 → [T,T,F,F]`; the "lifting" property is correctly realised via `order.take(k_max)`.
2. **Rank-IC = Spearman done right.** Average-rank transform (correct tie handling via mean tie-group position) then Pearson; monotone→+1, anti→−1, ties→>0.95, noise→≈0, dispersionless→None. `spearman_pvalue` uses the documented `z=ic·√(n−1)` normal approx.
3. **Leakage-safe (causal + OOF).** `forward_returns` sums `net[t+1..=t+h]` with last h bars NaN — strictly causal; IC restricted to caller-supplied disjoint fold index sets; pooled uses the two OOF folds' union. Design wires `qe-wfo::PurgedKFold` at the call site.
4. **Tests non-vacuous.** Net series genuinely driven by a hidden factor (`net[t]=0.9·hidden[t−1]+noise`); predictive→Admit, sign-flipped→Flag, noise→Drop — verdicts from real relationships, not fiat.
5. **Report-only confirmed** (3-file diff, unwired, nothing moved); **firewall clean** (`qe-validation→qe-determinism` only).

### Non-blocking follow-ups (accepted)
1. Leakage-safety is contingent on the caller passing purged/embargoed folds (the module can't enforce it); unit tests use plain contiguous folds — add a `PurgedKFold` integration test when wired.
2. `spearman_pvalue` uses the √(n−1) normal approx rather than the exact t-transform (fine at large n).
3. Enforced admission deferred (report-only) — follow-up to wire admission into catalogue construction (this is the GP-program consumer, QE-451).
