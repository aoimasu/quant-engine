# QE-211 — bootstrap→live in-process cutover — [Approved]

- **PR:** #62 — https://github.com/aoimasu/quant-engine/pull/62 (squash-merged)
- **Ticket:** QE-211 (`Phase: P2` · `Area: ③→④` · `Depends on: QE-210, QE-207`)
- **Branch:** `qe-211/bootstrap-live-cutover` (deleted post-merge)
- **Merged commit subject:** `QE-211: bootstrap→live in-process cutover (#62)`
- **Reviewed commits:** `b26665c` (pass 1) → `99bfc42` (pass 2, review-nit cleanup)
- **Evidence / design:** `docs/architecture/qe-211-bootstrap-live-cutover-design.md`
- **Changed files:** `crates/runtime/src/cutover.rs` (new), `crates/runtime/src/lib.rs`, design note.

### Goal
In-process replay→live handoff: switch the evaluator session in place from replay to wss continuation at
cutover; no state copy, no gap/overlap in evaluated bars.

### Acceptance criteria (from backlog)
- [x] Cutover produces no duplicated or skipped bar; post-cutover decisions match a continuously-running
  reference on a fixture — `cutover_matches_continuous_reference_with_no_dup_or_gap`.

### Implementation summary
- `Cutover` owns the warmed session (moved from `Reconstructed`, no copy). `feed_live_bar`: `open <= last`
  → `Duplicate`; `open == last + interval` → contiguous, `go_live()` in place once + `on_bar` → `Evaluated`;
  `open > last + interval` (or sub-interval misaligned) → `Gap`. `from_reconstructed` anchors on
  `decisions.last().time_ms` (`EmptyReplay` if none). `debug_assert` guards bar resolution == base.

### Verification (toolchain 1.96.0)
- fmt/clippy clean · `cargo test --workspace --locked` — 56 suites ok (qe-runtime: 42) · deny ok ·
  CI on PR #62: 4/4 passed, CLEAN.

### Review notes — [Approved] (two passes)

**Pass 1 (@ `b26665c`).** No blocking defects. Verified:
- AC proven with a fair, non-vacuous reference: reference = one session fed `0..K`, `go_live()` at K, then
  `K..N`; built identically to the cutover's booted session and fed identical bars `0..K`, so identical
  boundary state — the canonical continuously-running session. `cycling_genome(3)` gives real Enter/Hold/Exit;
  `assert_eq!(cut_live, ref_live)` compares full `EvalOutput` (time_ms, mode, decisions), proving mode==Live
  parity + no dup/no skip. Overlap-drop + gap detection exercised.
- In-place / no state copy real: `from_reconstructed` moves the non-Clone session; `go_live` relabels in
  place; one object end-to-end.
- Continuity math correct, no off-by-one; overlap returns `Duplicate` before `on_bar` (no double-advance);
  new bar can't be mis-classified; `interval_ms = base.minutes()*60_000`.
- Lazy `go_live` correct (guarded after the Duplicate early-return; duplicates keep mode Replay).
- Scope/firewall/parity clean: decoded-Bar seam (QE-205), netting deferred (QE-213), no new cross-crate edge,
  parity preserved by driving shared `on_bar`.

Non-blocking notes: (1) add `debug_assert!(bar.resolution() == base)`; (2) `Gap` also covers sub-interval
misaligned bars — worth a doc note.

**Pass 2 (@ `99bfc42`, delta `b26665c..99bfc42`) — [Approved] confirmed.** `cutover.rs` only:
- #1 `debug_assert!(bar.resolution() == self.base, ...)` added; never trips on the suite (all M5); fires only
  on a genuine mismatch. `debug_assert!` is a runtime `if cfg!(debug_assertions)` (not a `#[cfg]` strip), so
  `self.base` stays a real read in all profiles — no dead_code/`-D warnings` regression, zero release cost.
- #2 `Gap` doc expanded to cover sub-interval misalignment. No dep change → green gate on `99bfc42` valid.
