# QE-422 — Keyboard / screen-reader access for clickable table rows and universe chips — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/119 (squash-merged)
- **Branch**: qe-422/a11y-rows-universe-chips
- **Implementation commit**: e0451f5
- **Spec of record**: `docs/reviews/2026-07-15-team-improvement-review.md` → `### QE-422`
- **Evidence note**: `docs/architecture/qe-422-a11y-clickable-rows-universe-chips-design.md`

## Acceptance criteria
- [x] A run opens from the list via keyboard alone.
- [x] Universe symbols toggle via keyboard and announce as checkboxes.

## Verification
Local + CI green on e0451f5 (frontend-only): `npm run lint`/`build` (tsc -b && vite build)/`test` (vitest 59/59, 14 files). Rust required checks pass unchanged; frontend.yml green. No Rust touched; no golden changed.

## Review verdict — [Approved]
a11y assessed on correctness, not just green tests.

1. **Row keyboard-operable (core).** A clickable `<tr>` (only when `onRowClick` is set) gains `role="button"` + `tabIndex=0` + an `onKeyDown` that fires on Enter and Space/Spacebar, `preventDefault()`-ing Space (stops page scroll) and invoking the same `onRowClick(row, i)` as the click. Rows without a handler stay plain `<tr>` (no role/tabIndex/class). `role="button"` (vs `"link"`) is the honest choice for a generic handler with no `href` — activates on both Enter and Space. Visible focus via `:focus-visible` (no ring on mouse) with accent outline. Non-vacuous test: focus→Enter fires `(rows[0],0)`, Space fires again, a second row fires `(rows[1],1)`, no-handler table exposes no button/role/tabIndex.
2. **Chips are real checkboxes (core).** Each chip is now a native `<input type="checkbox">` inside a `<label>`, visually hidden via the clip technique (NOT `display:none`, so it stays focusable and in the a11y tree). Carries `checked` + `aria-label={symbol}`, so AT announces "checkbox, {symbol}, checked/unchecked" — strictly better than the removed fake `role="checkbox"` span. Space toggles natively (`onChange`→`toggleSymbol`); Enter is wired explicitly for parity (no double-fire). `:focus-within` rings the visible label. Non-vacuous test: `getByRole('checkbox',{name})`, Space toggles BTC off with ETH independent, Enter toggles back.
3. **Behavior parity.** `BacktestsList.tsx` untouched — its pre-existing `onRowClick={(row)=>onOpen(row.id)}` is now keyboard-reachable, nav still routes through `onOpen(row.id)`. Chip `onChange`→`toggleSymbol` add/removes in the Set and submit builds `universe` from selection; "keyboard-deselect ETH → `universe:['BTCUSDT']`" test proves the selection→payload path. No regression.
4. **Tag reuse safety.** `Tag.tsx` not in the diff — stays a non-interactive label; the chip uses `<Tag mono>` as pure decoration; the old fake `role="checkbox"`/`aria-checked`/`onClick` deleted, so nothing advertises undeliverable interactivity. Non-interactive usages (BacktestResult) unaffected.
5. **Scope.** DataTable is shared, but the change is gated entirely on `onRowClick` — `MarketData`/`BacktestResult` render byte-identically. QE-423 + QE-408 untouched. Blast radius: the two a11y surfaces + tests. No golden; frontend-only.

### Non-blocking (a11y follow-ups — none block the AC, which is fully delivered)
1. **`role="button"` on `<tr>` overrides the implicit `row` role** — keyboard-operable (the AC) but AT loses table-row/grid structure. Pragmatic/common; the more faithful pattern is a focusable control inside a cell or a full grid pattern. Tracked note if the tables later need real row/grid semantics.
2. **No explicit action label on the row button** — its accessible name is the concatenation of every cell's text (verbose, conveys no action). An optional generic API (e.g. `rowActionLabel?: (row,i)=>string` → `aria-label`, callers pass `Open run ${row.id}`) would announce the action clearly. Deferred as its own scoped follow-up (a generic-component API decision, not a one-liner); the row is still operable and named.
3. **Focus management not addressed** — after row activation the list navigates away and QE-410 live polling re-renders rows; neither restores/traps focus, so keyboard focus can land on `<body>` after a poll refresh. Minor; not test-covered.
4. **Focus-ring contrast unverified** — `var(--accent)` outline / `var(--ring)` shadow can't be checked in jsdom against the WCAG 3:1 non-text bar vs `--surface-hover`; worth a one-off manual check in both themes.
5. **No disabled/loading affordance** for rows/chips during submit — out of scope, noted for completeness.
