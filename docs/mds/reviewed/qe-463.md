# QE-463 — Real http ingest decoder for Binance USDT-M (behind http feature) — Review record

- **Ticket:** QE-463 · **Branch:** `qe-463/http-ingest-decoder` · **PR:** https://github.com/aoimasu/quant-engine/pull/176
- **Merged commit (branch head):** `ca6cd9e`
- **Base:** `main`
- **Evidence note:** [`docs/operations/qe-463-http-ingest-decoder-analysis.md`](../../operations/qe-463-http-ingest-decoder-analysis.md)
- **Ticket detail:** [`docs/mds/tickets/QE-463.md`](../tickets/QE-463.md)
- **Design ref:** `docs/architecture/qe-455-research-flow-design.md` §8.1, §8.5

### Green-gate (all on commit `ca6cd9e`)
Feature OFF: `cargo fmt --all --check` PASS · `clippy --workspace --all-targets -D warnings` PASS · `cargo test --workspace` PASS (169) · firewall PASS · `cargo deny check` PASS
Feature ON: `cargo clippy -p qe-ingest -p qe-cli --features http --all-targets -D warnings` PASS · `cargo test -p qe-ingest -p qe-cli --features http` PASS (170)
No-new-TLS-deps: `ring`/`rsa`/`rustls` ABSENT (independently confirmed via `cargo tree -i` → "did not match any packages"). Transport stays `native-tls → ureq → system OpenSSL` (already allow-listed).

### Acceptance criteria — all met
- [x] Under `--features http`, decodes Binance USDT-M klines + funding, closed windows only (excludes forming right-edge bar + in-progress funding), incrementally (skips covered vs `coverage_bounds`), resumably (continues from coverage end), idempotently (re-fetch = byte-identical no-op).
- [x] Calibration honesty: klines-only vintage always records `CalibrationSource::Uncalibrated`; premium/OI/mark left empty (never fabricated); open_interest/mark_price drop documented. (Inspector surfacing deferred to QE-464/QE-467 — ruled acceptable, see below.)
- [x] Internal gaps within `[first,last]` detected (missing-key scan) + back-filled (not just edge-extend).
- [x] `ureq` feature set adds no `ring`/`rsa`/rustls (asserted via `cargo tree -i`).
- [x] Feature off: crate/CLI build + offline tests unchanged; no real network call reachable in default build.
- [x] Decode logic exercised offline against a fixture (FakeRest, no sockets).

### Review verdict — Approved (reviewer, commit `ca6cd9e`)

Meets all AC; no blocking issues. Verified against QE-463.md AC, design §8.1/§8.5, the diff, and independent FEATURE-ON
builds/tests (default `cargo test` cannot exercise gated code): `--features http` 170 passed, feature-off 169, clippy clean.

1. **Default-off / offline airtight — CONFIRMED.** `HttpRestSource` + all `ureq` usage entirely `#[cfg(feature="http")]` (`rest.rs:163-210`); the CLI adapter is gated too. Decode/plan/closed-window logic is ALWAYS-compiled pure functions over an injected `RestSource` port, so no real-network symbol is reachable in the default build. Every test offline (in-memory `FakeRest`; even the http-gated end-to-end test drives `run_ingest`→real `MarketStore` through FakeRest — no socket). `--synthetic`/`run_ingest` untouched.
2. **Decode/plan invariants genuinely proven — CONFIRMED.** `closed_klines`/`closed_funding` exclude the forming right edge (in isolation + end-to-end); `plan_missing` proves incremental-skip, internal-gap (`fetch_backfills_internal_gap_only` punches an interior hole and asserts exactly `[5*M5]` re-fetched), resume-tail-only, and idempotent (fully-covered ⇒ `plan_missing==[]` ⇒ no REST call by construction + byte-identical decode).
3. **No new TLS deps — CONFIRMED independently** (`cargo tree -i ring|rustls|rsa` all absent; `native-tls`/system OpenSSL). `cargo deny` green.
4. **Calibration honesty — CONFIRMED.** `IngestedWindow.calibration_source` always `Uncalibrated`; premium/OI/mark left empty; documented drop. Empty series are honest emptiness, never a default dressed as measured.
5. **Code quality — CONFIRMED.** Rate limits (429/418 + Retry-After), 5xx retry, pagination via the existing Backfiller, no unwrap/panic on malformed data.

**Ruling on the calibration-surfacing split: ACCEPTABLE (not blocking).** The honest marker exists at the right layer (`IngestedWindow.calibration_source` — public typed field, test-covered, adapter-asserted); durable recording into coverage/lineage + inspector surfacing genuinely belong to QE-464 (coverage-provenance thread) + QE-467 (lineage), explicitly out of scope. The whole http adapter is gated scaffolding QE-464 will wire — the marker is a live seam, not dead code.

### Non-blocking notes (do not block merge)
1. **HANDOFF TO QE-464:** the `calibration_source` marker does NOT flow through `HistoricalWindow` (dropped after a `debug_assert`) — **QE-464 must re-attach it** into the coverage/provenance thread. Trivial, but a cleaner seam would thread it.
2. The idempotent test proves no-op via empty-result rather than an explicit REST-call counter.
3. Minor naming smell: `parse_klines_json` also parses funding rows.
