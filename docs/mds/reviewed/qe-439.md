# QE-439 — Coherent DSR trial basis + log-N expected-max-sharpe fix — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/141 (squash-merged)
- **Branch**: qe-439/dsr-trial-basis
- **Implementation commit**: 8493f73
- **Spec of record**: `docs/reviews/2026-07-16-maxdama-panel-review.md#qe-439` (+ QE-450 design note §5)
- **Evidence note**: `docs/architecture/qe-439-dsr-trial-basis-design.md`

## Acceptance criteria
- [x] **Log-N fix:** `expected_max_sharpe` finite (≈8–13) at N up to 1e20 via a `√(2 ln N)` log-space path; the `+∞` degeneracy is gone; small/moderate-N exact path unchanged.
- [x] Basis coherent + documented; erring conservative (over-deflate safe; never under-deflate).
- [x] Green gate + firewall green; no golden moved; GP-scale machinery explicitly deferred to QE-451.

## Implementation
`crates/validation/src/dsr.rs`: `expected_max_sharpe_ln(ln_n)` log-space path, rerouted from `expected_max_sharpe` when the tail underflows (`p1 >= 1.0 || p2 >= 1.0`); small-N exact path untouched; `effective_trials` documented as the conservative analytic floor `cells·gens·windows`. Headline: N=1e20 → **9.32** (was `+∞`); `usize::MAX` → 9.14.
**Deferred to GP program (QE-451):** distinct-canonical N + `distinct_evaluations`; complexity-stratified `trial_sharpe_variance`; full-population uncensored `variance_returns`/PBO; label-shuffle κ null — all require a GP evaluation ledger / `ExprTree` node counts that don't exist yet.

## Verification (LOCAL green gate re-run by reviewer on `8493f73`, CI disabled — all PASS)
fmt · clippy locked + all-features `-D warnings` · `cargo test --workspace --locked` (**837 passed, 2 ignored**) · deny · firewall · dsr (9). No golden moved.

## Review verdict — [Approved] (0 blocking, 2 non-blocking)
1. **Log-N math correct + continuous (CRITICAL) — reviewer replicated.** Implements BdP `√V·((1−γ)Φ⁻¹(1−1/N)+γΦ⁻¹(1−1/(Ne)))` in log space via Acklam's tail rational on `q=√(2 ln N)` (canonical constants; correct `Φ⁻¹(1−p)=−Φ⁻¹(p)` symmetry). Finite everywhere (9.32 @1e20, 9.14 @usize::MAX). Exact path bit-identical (`normal_ppf(1−1/n)`). **Continuity proven both sides:** at N≈2⁵³, `1−(1−1/N)==1/N` still exact ⇒ exact path's internal `q=8.5717` identical to the log path's — no jump (monotone-across-switch test).
2. **Conservative direction confirmed.** Bar monotone in N; log path agrees with exact to <1e-5 in the overlap and is the more-accurate side (avoids `1−1/N` cancellation) so never below the true value; sits correctly below the looser `√(2 ln N)` asymptotic. The `+∞` was a degenerate bug (DSR≡0); the fix restores the correct finite value. System conservatism = documented gens/windows over-count (safe false-reject).
3. **GP deferral legitimate.** Grep-confirmed no `ExprTree`/`distinct_evaluations`/evaluation-ledger/node-count infra exists — on the hand catalogue there is nothing to count. The panel itself called the gens/windows basis "conservative, a tightening not a live risk." Honest scoping.
4. **No golden moved** (fixture N tiny → byte-identical exact path; DSR on sidecar); **firewall clean** (`qe-validation → qe-determinism` only).

### Non-blocking follow-ups (accepted)
1. `normal_ppf_upper_tail_from_ln` copies Acklam's C/D tail coefficients locally rather than sharing with `stats::normal_ppf` — expose a shared `ppf_from_ln_tail` in `stats` to remove duplication/drift risk.
2. Worth stating the fix is inert on the current hand-catalogue (N~1e7) and only load-bearing at GP scale — which is exactly why no golden moves. (The GP consumer is QE-451.)
