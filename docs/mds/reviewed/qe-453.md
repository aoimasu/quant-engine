# QE-453 — admin SPA `evolve/` area (NewCampaign / CampaignMonitor / FormulaSexpr / PoolReview / PoolBrowser) — review record

*Frontend ticket — the `web/src/app/evolve/` area, mirroring `web/src/app/training/`, consuming the merged QE-452 endpoints.*

- **PR**: https://github.com/aoimasu/quant-engine/pull/157 (squash-merged)
- **Branch**: qe-453/spa-evolve-area
- **Implementation commit**: `8960244` (fix pass; round-1 was `4046ea0`)
- **Spec of record**: `docs/architecture/qe-450-gp-indicator-evolution-design.md` §13.4 (screens), §13.5 (endpoints), §13.3 (two lifecycles), §13.6 (PoolReview gate = safety control)
- **Evidence note**: `docs/architecture/qe-453-spa-evolve-area-design.md`
- **Builds on (merged)**: QE-452 Phase A + B (consumed endpoints), QE-451 GP engine
- **NOTE**: frontend ticket — the green gate is the `web/` toolchain (typecheck/lint/test/build), NOT cargo. Frontend-only: no Rust/golden change.

## Acceptance criteria — all met
- [x] **NewCampaign**: launch an `evolve` run (mirror Training's NewRun); seed REQUIRED (blocks submit); client caps matching `validate_evolve` (depth≤4/nodes≤16/lookback≤200/windows∈{5,10,20,50,100}/K≤16); mode ∈ {sandbox,production}.
- [x] **CampaignMonitor**: MAP-Elites heatmap + trial-count-vs-bar from `GET /api/runs/{id}/archive`; run status; Halt (POST `/halt`).
- [x] **FormulaSexpr**: canonical S-expression rendered readably (the human-inspectable form).
- [x] **PoolReview — THE GATE**: K formulas + deflation summary + lineage + lifecycle; Approve/Reject/Revoke/Seal wired to the governance endpoints; honestly reflects the server fail-closed reality (production Seal 409 surfaced, not faked); illegal-from-state actions disabled/omitted.
- [x] **PoolBrowser**: list/browse pools (`GET /api/formula-pools`) with lifecycle, linking into PoolReview / FormulaSexpr.
- [x] **Boundaries**: no new server endpoints / no Rust change (consume existing QE-452 routes only); mirrors `web/src/app/training/`.
- [x] **Web green gate** (typecheck + lint + test + build) all green; only `web/` (+docs) changed.

## Implementation
- **NewCampaign** — `POST /api/runs {type:"evolve"}` (`createEvolveRun`); mirrors `NewTraining` form + `validate()`; seed REQUIRED (test-pinned), caps + window lattice matching `validate_evolve`.
- **CampaignMonitor** — `usePollingRun` on `GET /api/runs/{id}` + `/archive` + `POST /halt`; mirrors `TrainingMonitor`: heatmap (coverage-grid pattern) + trial-count-vs-bar + mode banner + Halt.
- **FormulaSexpr** — canonical S-expression in an `overflow-x:auto` mono container + hash.
- **PoolReview (gate)** — `GET /api/formula-pools/{id}` + governance POSTs; `lifecycle` renders only from server-fetched detail (no optimistic mutation → a fake `sealed` is impossible); `act()` re-fetches on both success and error; Seal legal only from `approved` (mirrors the server state machine).
- **PoolBrowser** — `GET /api/formula-pools`, mirrors `TrainingList` (`DataTable` + lifecycle/mode/gp-aware badges).

## Review — two rounds
**Round 1 (`4046ea0`): [Reviewed], 1 blocking.** Substantively excellent — reviewer read PoolReview + NewCampaign + tests directly and swept the remaining screens (via a subagent) for any fake-success/hidden-refusal pattern; all clean:
- **PoolReview honest by construction** — lifecycle from server-fetched detail only; the 409 test is non-vacuous (asserts BOTH the message shown AND lifecycle stays `approved`); illegal actions omitted; Seal only from `approved`.
- **NewCampaign** — seed genuinely required (test-pinned), caps match `validate_evolve`, windows locked to the lattice.
- **Boundary clean** — web/+docs only, no `.rs`/Cargo/golden/`.snap`, no invented endpoints (all six API calls map onto merged QE-452 routes).
- **Mirrors Training** — shared `usePollingRun`/`useRunListPolling`/`DataTable`, same area/routing pattern.
- **BLOCKING**: the web gate was RED — `npm run lint` failed on `CampaignMonitor.test.tsx:73` (unused `_init` param, `@typescript-eslint/no-unused-vars`, no `^_` exemption). The implementer's "lint clean" note was stale (the `_init` was added to fix a typecheck error after the last lint run). Sent back.

**Round 2 (`8960244`) delta re-review: [Approved], 0 blocking.** `git diff --stat 4046ea0..8960244` = `CampaignMonitor.test.tsx | 2 +-` (test-only). The halt-test fetch mock is now `vi.fn<typeof fetch>(async (input) => …)` — typing the mock as `typeof fetch` keeps `.mock.calls` as the full `[input, init?]` tuple, so the halt assertion still verifies `init?.method === 'POST'` to `/api/runs/evolve-1/halt` (NOT weakened), just without the unused impl param. Reviewer re-ran the full web gate on `8960244` from `web/`: `tsc -b` exit 0, `npm run lint` exit 0 ("No issues found", swept for other unused-vars — none), `npm run test` 75/75 exit 0 (halt test passes → assertion holds), `npm run build` exit 0. Delta clean (no Rust/golden/`.snap`).

## Verification (LOCAL web green gate re-run by reviewer on `8960244`, CI disabled — all PASS)
`npx tsc -b` (exit 0) · `npm run lint` (exit 0, "No issues found") · `npm run test` (**75 passed / 21 files**, exit 0) · `npm run build` (exit 0).

### Non-blocking notes (accepted)
1. `CampaignMonitor` swallows non-404 archive errors (minor — the heatmap just stays empty; acceptable).
2. NewCampaign leans on the server to refuse production launches (the honest choice — client doesn't fake a client-side block the server owns).

### Deferred to QE-454 (server-side)
Authoritative RBAC/`require_role`, server-authoritative `seal_allowed`, `DEFLATION_BASIS_VERSION` unlock const, tamper-evident audit + `GovernanceRecord`, dual sign-off, and the QE-451-Phase-1b `N*` deflation fold-in. The SPA honestly reflects today's 403/409 fail-closed reality.
