# QE-467 — Persist seal evidence + net-of-cost holdout series + provenance (vintage format 7→8) — Review record

- **Ticket:** QE-467 · **Branch:** `qe-467/persist-seal-evidence` · **PR:** https://github.com/aoimasu/quant-engine/pull/168
- **Merged commit (branch head):** `303477195682427f81ffe2962cacaf6e09f39b12` (was `93929c8`; +comment-only fixes for the 2 non-blocking notes, gate re-run green)
- **Base:** `main`
- **Evidence note:** [`docs/operations/qe-467-persist-seal-evidence-analysis.md`](../../operations/qe-467-persist-seal-evidence-analysis.md)
- **Ticket detail:** [`docs/mds/tickets/QE-467.md`](../tickets/QE-467.md)
- **Design ref:** `docs/architecture/qe-455-research-flow-design.md` §4.1, §10

### Green-gate (all on commit `3034771`, re-run after comment fixes)
- `cargo fmt --all --check` — PASS
- `cargo clippy --workspace --all-targets --locked -- -D warnings` — PASS
- `cargo test --workspace --locked` — PASS (1053 passed, 3 ignored)
- `cargo test -p qe-architecture --test firewall --locked` — PASS
- `cargo deny check` — PASS

### New public schema/types (all `crates/vintage/src/lib.rs` unless noted)
- `SealEvidence` (lib.rs:62), `HoldoutReturnSeries` + `handle()` (lib.rs:102/113), `DataProvenance` (lib.rs:124),
  `TimeRange` (lib.rs:137), `HoldoutSplit` (lib.rs:147), `RegimeShare` (lib.rs:161), `SteerDelta` (lib.rs:171),
  `ResearchProvenance` (lib.rs:194); new `VintageContent` fields `seal_evidence`/`holdout_series`/`provenance`
  (lib.rs:259/263/268); `crates/server/tests/regenerate_fixture.rs` (ignored, real-seal regenerator).

### Acceptance criteria (from `docs/mds/tickets/QE-467.md`) — all met
- [x] Sealed `VintageContent` carries full seal evidence (IC/FDR, cost-stress min{1×,2×} net, realised turnover,
  `capacity_usd`) alongside existing DSR/SPA/uncensored-PBO, all content-addressed.
- [x] A canonical net-of-cost holdout return series on the DEPLOYED capacity-capped weights is persisted per
  vintage and addressable by handle; never gross/equal-weight/lone-Sharpe.
- [x] `VintageContent.lineage` carries hashed `data_provenance ∈ {real,synthetic,mixed}` plus holdout split,
  holdout regime composition, per-holdout consultation count, and steer-delta fields (schema defined here;
  populated downstream).
- [x] Single bump / golden safety: `VINTAGE_FORMAT_VERSION` is `8`; goldens regenerated via the real seal path;
  `== 7` assertion updated to `== 8`; QE-006 determinism harness green with no unrelated hash drift.
- [x] No `qe-vintage → qe-wfo`/`qe-ensemble` code edge added; firewall/dependency-topology test stays green.

### Review verdict — Approved (reviewer, commit `93929c8`; re-confirmed `3034771`)

> **Non-blocking comments resolved at `3034771`** (comment-only in `crates/cli/src/jobs/train.rs`, no logic/golden change): (1) `capacity_usd` comment corrected to the accurate `hash_stable` ×1e12-overflow-past-2^53 rationale, noting serde_json/ryu round-trips any finite f64; (2) `cost_stress_net_min` gains the seal-time / not-a-G1-criterion clarification. Both comments verified accurate; verdict stands Approved.

Meets all five acceptance criteria; no blocking issues. Verified against QE-467.md AC, design §4.1/§10, and the diff.

**AC verification**
1. **Full seal evidence, content-addressed** — `SealEvidence` (DSR/PBO/SPA/n_trials + realised_turnover + capacity_usd, plus optional cost-stress-net / uncensored-PBO / IC / FDR slots) is a `VintageContent` field inside the hashed content. `seal_evidence_is_part_of_the_hash_and_round_trips` proves a changed DSR or capacity moves the vintage id and that the block round-trips through disk verify. PASS.
2. **Net-of-cost holdout series on DEPLOYED capacity-capped weights** — `holdout_series` = `train.rs:480` `holdout_returns = combine(&chromosomes, &weights, holdout_bars, &train_cfg)` where `weights` come from `capacity_capped_weights` (`train.rs:463`); it is the exact net-of-cost series the gate's DSR/robustness consumed (not gross/equal-weight/lone-Sharpe), hash_stable-rounded, content-addressed, and addressable via `HoldoutReturnSeries::handle()` (SHA-256, 64-hex). PASS.
3. **Provenance + extended lineage schema** — `ResearchProvenance { data_provenance ∈ {real,synthetic,mixed}, holdout_split, regime_composition, consultation_count, steer_delta }` defined here and hashed; the train path writes `data_provenance = Real` and leaves the flow's fields at defaults/None (correct for QE-467 — QE-458/QE-460 populate downstream). `provenance_is_part_of_the_hash_and_downstream_fields_round_trip` proves real→synthetic flips the id and that the deferred fields populate + round-trip under this bump. PASS.
4. **Single 7→8 bump / golden safety** — exactly one `VINTAGE_FORMAT_VERSION` 7→8 bump; the `== 7` assertion updated to `== 8` (`lib.rs:771`). Goldens regenerated via the real seal path: `regenerate_fixture.rs` (`#[ignore]`, dev-only) seals through `Vintage::seal`, and I independently recomputed SHA-256 over the new `sample_vintage.json` content — it matches the committed `dc33f337…` hash (a hand-edited hash would not). Only the sample-vintage hash + format_version moved; no unrelated drift. `qe-vintage` tests green (18/18). PASS.
5. **Firewall** — `qe-vintage` gains no `qe-wfo`/`qe-ensemble` edge (stores plain `f64`s only); the server's new `qe-determinism`/`qe-risk`/`qe-signal`/`rust_decimal` deps are under `[dev-dependencies]` (fixture regenerator), excluded from the production firewall graph. Firewall reported green. PASS.

**Implementer's flagged choice (`ic`/`fdr` = `None` on train) — ACCEPTED, not a gap.** Verified the ensemble train G1 gate produces no IC/FDR scalar: `evaluate_g1` (`crates/gate/src/lib.rs`) evaluates only samples/edge/DSR/SPA/oos-tolerance/PBO, and `RobustnessReport` (`crates/validation/src/lib.rs:109`) carries no `ic`/`fdr`. QE-434 rank-IC + BH-FDR is a per-indicator factor-admission screen (`crates/validation/src/ic.rs`) invoked only from the GP/evolve path (`crates/wfo/src/gp/gates.rs`), never from `train.rs`. Populating a scalar here would be NEW evidence, which the AC ("persist the gate's own outputs — do not recompute or re-decide") forbids. This correctly parallels `GateSnapshot::uncensored_pbo` being `None` on the train path. The schema slots are the right home for the evolve-path values the inspector reads.

**Float round-trip fix — sound.** `capacity_usd = deployed_capacities.sum().round()` (whole dollars) is deterministic, negligible precision loss on a multi-million figure, and an integer-valued f64 (≪ 2^53) round-trips through serde_json exactly. The `3034771` comment fix restates the accurate rationale (`×1e12` overflowing 2^53 in `hash_stable`, not a serde_json round-trip failure). Correct.
