# QE-447 — Pre-trade %ADV participation guard — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/149 (squash-merged)
- **Branch**: qe-447/participation-guard
- **Implementation commit**: 387a613
- **Spec of record**: `docs/reviews/2026-07-16-maxdama-panel-review.md#qe-447` (panel P3/latent)
- **Evidence note**: `docs/architecture/qe-447-participation-guard-design.md`

## Acceptance criteria
- [x] Pre-trade %ADV guard rejects an order whose participation `(order_qty / ADV)` exceeds a configured threshold; within passes.
- [x] Default OFF (`max_participation: None`) ⇒ byte-identical, no golden moved; zero/None/overflow ADV fails safe (no panic, fail-closed).
- [x] Panic-free on the order path (QE-268 deny lint); green gate + firewall green.

## Implementation
New `LimitKind::MaxParticipation` (default outcome `Reject`, rides `Halt>Reject>Clamp` reducer) + `RiskLimits.max_participation: Option<Fraction>` (default None) — sibling pattern of `MaxGrossExposure`/`MarginUtilisationCeiling`. ADV (dollars, market liquidity not account capital) passed via a new `PreTradeGovernor::with_adv(Option<Notional>)` builder; `new()`/existing call sites untouched. Participation `= |order notional| / ADV` via `Decimal::checked_div`; degenerate ADV fails closed → Reject; flat order skips division. Exact `rust_decimal`.

## Verification (LOCAL green gate re-run by reviewer on `387a613`, CI disabled — all PASS)
fmt · clippy locked + all-features `-D warnings` · `cargo test --workspace --locked` (**894 passed, 2 ignored**) · deny · firewall · participation (4) + limit (6). No golden moved (regenerate → empty diff).

## Review verdict — [Approved] (0 blocking, 2 non-blocking)
1. **Panic-free (CRITICAL, compile-enforced).** `pretrade.rs:23` carries the QE-268 deny lint, clippy `-D warnings` green; division is `mag.checked_div(adv)` (never panicking `/`); no unwrap/expect/panic/indexing.
2. **Fail-closed on degenerate ADV.** Configured cap + live order (mag>0): ADV None/≤0/overflow → Reject; flat order (mag=0) skips division and passes. Matches sibling fail-closed convention. Non-vacuous tests (None/0/-1000/flat).
3. **Default-OFF byte-identical.** `None` → guard inert (1e6 order sent unchanged); `RiskLimits` in no golden/fixture and not in `VintageContent` → no hash can move; diff = 3 files; regenerate → empty.
4. **Correct + sibling pattern.** Over cap → Reject with participation in detail (sign-agnostic); `Halt>Reject>Clamp` reducer confirmed (participation Reject overrules a co-occurring clamp — composes with QE-215 caps).
5. **ADV seam deferral honest** (via `with_adv`, no live feed today → latent P3 rail); **firewall clean** (hedger+risk, no Cargo change).

### Non-blocking follow-ups (accepted)
1. **Fail-closed coupling:** because the guard fails closed, configuring `max_participation` **without** an ADV feed wired would Reject **every** live order (brick trading). Correct fail-safe direction, inert today (both default off), but **enabling the cap requires wiring the rolling-ADV feed in the same change** — the follow-up that adds the live ADV feed should add config-validation rejecting `max_participation = Some` without an ADV source. (Same live-wiring cohort as the other deferred feeds.)
2. `max_participation` lacks `#[serde(default)]` — pre-existing struct-wide pattern for all sibling `Option` fields; non-issue (`RiskLimits` in no golden).
