# QE-424 — Frontend resilience: error boundary + tests for the list/401/deep-link seams — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/121 (squash-merged)
- **Branch**: qe-424/frontend-resilience
- **Implementation commit**: fe4fdbb
- **Spec of record**: `docs/reviews/2026-07-15-team-improvement-review.md` → `### QE-424`
- **Evidence note**: `docs/architecture/qe-424-frontend-resilience-design.md`

## Acceptance criteria
- [x] A thrown render error shows the fallback, not a blank page.
- [x] New tests cover the four seams and FAIL against today's behaviour where a bug exists (seam 4 empirically verified: dropping `initialVintage` fails ONLY seam 4).

## Verification
Local + CI green on fe4fdbb (frontend-only): `npm run lint` (0/0); `npm run build` (tsc -b && vite build); `npm test` (vitest 64/64, 16 files, +4). No Rust; no golden.

## Review verdict — [Approved]
1. **Error boundary (core).** `ErrorBoundary` is a dep-free class (`getDerivedStateFromError` sets error state; `componentDidCatch` logs `console.error` diagnostics only — no telemetry dep; no `package.json` addition). It wraps only the authed `<AppShell>`; the loading/unauth returns are earlier in `App()`, so a screen throw can never blank Login. A render throw yields a `role="alert"` fallback (inline-styled with CSS vars, renders even if a design primitive threw) — not a blank page. Recovery proven both ways: the boundary test flips the fault off, clicks "Try again" (`reset` clears state → children re-render, alert gone), and a second test drives the `resetKeys` auto-clear path; `App.tsx` uses `resetKeys={[me?.email]}` for auth-change recovery. `keysChanged` uses element-wise `Object.is`.
2. **Seams fail-on-regression (core).** All four covered, each failing on regression:
   - **Seam 1 (mixed-type filtering, QE-408)** — new App-level test (App→AppShell→BacktestsArea→BacktestsList) feeds a mixed `{runs:[backtest,train]}` page and asserts the train id/label absent and `tbody tr` count exactly 1; dropping the `r.type==='backtest'` filter surfaces the train row (count 2) and fails.
   - **Seam 2 (401→Login)** — referenced, adequate: `App.test.tsx` is a genuine App-level test (authed shell mounts, mid-session poll 401s, Login remounts in-place) plus the QE-409 hook test; removing the `onUnauthorized` subscription fails it.
   - **Seam 3 (list auto-refresh)** — referenced `BacktestsList.test.tsx` (running row 42%→…→SUCCEEDED, polling then stops). The polling seam lives entirely in the component (`useRunListPolling`); the App wrapper adds no polling logic, so component-level exercises the real mechanism. Reference-not-duplicate acceptable.
   - **Seam 4 (router-less deep-link)** — genuinely NEW, empirically verified non-vacuous. End-to-end exercise of `App.tsx`'s `openBacktestForVintage → backtestVintage → BacktestsArea initialVintage → NewBacktest`: render `<App/>` → Training nav → open succeeded `train-1` → "Backtest this vintage" → assert the New-backtest Vintage select equals the sealed `v-sealed-1`. The vintage list is ordered `[v-other, v-sealed-1]` (SEALED not first), so preselect passes only if the deep-link propagates. Confirmed: replacing `initialVintage={backtestVintage}` with a bare `<BacktestsArea/>` makes ONLY seam 4 fail (falls back to `v-other`) — precisely targeting the wiring.
3. **Scope.** Frontend-only. The `App.tsx` diff is purely the boundary wrap (JSX identical, re-indented). Blast radius = `ErrorBoundary.tsx` + `App.seams.test.tsx` + the one-line wrap. QE-406/408/409/410 contracts respected.
4. **Fallback a11y / no CI leakage.** Fallback is `role="alert"` with a labelled `type="button"` "Try again". `vitest.setup.ts` has no `console.error`→fail guard, so the boundary's logging can't fail CI; the per-test spy is clean-output hygiene, scoped + restored.
5. **No golden / frontend-only.** Diff entirely under `web/` + `docs/`.

### Non-blocking
1. Seam 3 is covered only at the component level (`BacktestsList.test.tsx`), not App-level — defensible (polling is wholly component-owned; the App wrapper adds none) and matches "reference not duplicate", but seams 1 and 4 got new App-level tests while 3 did not. An App→list auto-refresh integration test would be the gap-filler if poll orchestration ever moves up to `App`.
2. `resetKeys={[me?.email]}` clears on identity change but not on in-app screen navigation — a screen that threw stays on the fallback until "Try again" or an auth change (`active` isn't a reset key). Arguably the safe default (don't silently retry a broken screen); adding `active` to `resetKeys` would make nav dismiss a stale error if product wants it. Design choice, not a defect.
