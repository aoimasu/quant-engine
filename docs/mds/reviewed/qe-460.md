# QE-460 — RunSpec::Flow composite run-kind + frozen-holdout carve (protocol 2→3) — Review record

- **Ticket:** QE-460 · **Branch:** `qe-460/runspec-flow` · **PR:** https://github.com/aoimasu/quant-engine/pull/173
- **Merged commit (branch head):** `b505661` (feat `f8297a5` + cost-parity fix `b505661`)
- **Base:** `main`
- **Evidence note:** [`docs/operations/qe-460-composite-flow-analysis.md`](../../operations/qe-460-composite-flow-analysis.md)
- **Ticket detail:** [`docs/mds/tickets/QE-460.md`](../tickets/QE-460.md)
- **Design ref:** `docs/architecture/qe-455-research-flow-design.md` §5, §4

### Green-gate (all on commit `b505661`)
- `cargo fmt --all --check` — PASS · `clippy -D warnings` — PASS · `cargo test --workspace` — PASS (0 failed)
- firewall — PASS · `cargo deny check` — PASS · backend-only (no web/)
- PROTOCOL_VERSION==3 (assertion + agreement bytes updated); NO `VINTAGE_FORMAT_VERSION` bump (stays 8, only populates QE-467's `ResearchProvenance`); NO `evaluate_g1`/search/DE/seal change; QE-006 determinism green; un-flow vintages byte-identical.

### Acceptance criteria — all met
- [x] `type:"flow"` → one supervised run sequencing train→backtest atomically w/ content-hash vintage handoff; failed-G1 seals nothing & runs no backtest; one status row (`flow_sequences_train_then_backtest_and_records_sub_runs`, `flow_with_failed_g1_train_runs_no_backtest_and_fails`).
- [x] Backtest is the single recorded holdout consultation, no independent deflation credit; re-surfaces the gate's holdout verdict (now at the gate's own fee — see cost-parity fix).
- [x] Holdout regime-stratified (≥K labels) / multi-fold WFO, floored (`400`), embargoed, edge server-derived; split + regime composition in `VintageContent.lineage`.
- [x] Consultation counter increments overlap-keyed (intersection, not equality) — `flow_consultation_counter_is_overlap_keyed_not_equality`.
- [x] Train + backtest pin the same sealed cost calibration (asserted equal — fee + slippage).
- [x] Re-run from seed + pinned snapshot → byte-identical vintage (`flow_seal_is_byte_identical_for_the_same_seed_in_a_fresh_repo`).
- [x] `PROTOCOL_VERSION`→3 (assertion updated); no 2nd `VINTAGE_FORMAT_VERSION` bump; firewall green (no new cross-crate edge — opaque `result.json` handoff).

### Review verdict — Approved (first pass `f8297a5` Reviewed→blocking; re-review `b505661` Approved)

**First pass (`f8297a5`) — Reviewed (blocking, 1):** the composite lifecycle, atomicity, overlap-keyed counter, regime carve, determinism, protocol bump, and firewall were all correct and well-tested, but AC-5 cost-parity was asserted against the WRONG baseline: the flow's holdout backtest pinned `FLOW_TAKER_FEE_BPS = 2.0` (2 bps) while the train gate priced the G1 holdout at `FeeSchedule::default().taker = 5 bps` — the flow re-costed the single consultation 2.5× cheaper (a friendlier friction, breaching AC 5 / maxdama #6 and tainting AC 2, since the flow-backtest number was rosier than the gate's holdout verdict). The `flow_cost_parity_ok` guard + its rejection test gave false assurance by baselining on the wrong 2 bps.

**Flagged defaults — ruled ACCEPTABLE (non-blocking):** K=2 min regime labels (`MIN_HOLDOUT_REGIMES`), N=2 folds, holdout floor 250, embargo 1 — all enforced (K<2 hard error; floors → 400) and documented; consistent with the QE-458 `MIN_OCCUPIED_NICHES` precedent. Product should confirm K=2; it does not block.

**Re-review (`b505661`) — Approved. Cost-parity defect closed drift-proof.** The fee is DERIVED at source — `gate_taker_fee_bps = decimal_to_bps(train_cfg.friction.fees.taker)`, the exact `train_cfg` whose `holdout_returns` feed `evaluate_g1` (→ 5 bps) — carried as a REQUIRED `result.json` handoff field (missing → flow fails cleanly, no silent default), and pinned into `flow_backtest_params` + `flow_cost_parity_ok`. The wrong `FLOW_TAKER_FEE_BPS = 2.0` const is deleted; no 2-bps path remains. So the flow's single holdout consultation re-costs at the gate's own 5 bps — AC 5 parity holds, AC 2 restored. (Server can't import `qe-wfo` per firewall → derive-in-cli + carry-via-opaque-JSON keeps the firewall green.) New drift-proof test `flow_records_the_gate_taker_fee_and_it_equals_the_gate_default` (real `--flow` run, asserts recorded fee == `FeeSchedule::default().taker × 10_000` == 5.0); `flow_cost_parity_rejects_a_friendlier_friction_model_than_the_gate` now baselines on 5 bps and rejects the old 2 bps. Golden-safety holds (fee is `Option`, `--flow`-only, in `result.json` not the vintage hash) — plain and flow sealed vintages byte-identical; only the flow backtest number moves (correctly). All previously-verified-good items untouched. Re-ran cli flow 4/4, server flow 9/9, firewall 1/1, determinism 18/18, protocol 16/16, backtest goldens 2/2 — all green.

### Non-blocking carry-overs (do not block merge)
1. `consultation_count` is in the vintage content hash, so byte-identical reproduction is conditional on campaign/repo state, not seed+snapshot alone (a QE-467 schema property).
2. `holdout_window` is date-granular vs the bar-precise carve — the only residual approximation on the re-surfaced number (v1 limitation).
3. Flagged defaults (K=2 etc.) acceptable; product sign-off on K wanted.

### Follow-up closed
QE-458's deferred AC(d) "stress-regime + OOS-span-in-bars" enforcement is now met FOR THE FLOW PATH (holdout geometry keyed on named QE-125 regimes ≥K + `HOLDOUT_FLOOR` bars). Plain-`train`'s separate QE-458(d) deferral remains open (out of scope here).
