# QE-402 — Pin catalogue identity in the vintage; assert exact schema on load — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/104 (squash-merged)
- **Branch**: qe-402/catalogue-schema-pin · **Supersedes**: QE-262
- **Implementation commit**: 7b2858c
- **Spec of record**: `docs/reviews/2026-07-15-team-improvement-review.md` → `### QE-402`
- **Evidence note**: `docs/architecture/qe-402-catalogue-schema-pin-design.md`

## Acceptance criteria
- [x] Vintage sealed under catalogue vN fails to load against a vN+1 (same-width) build — hard `SchemaMismatch`.
- [x] Reordering two indicators changes the persisted catalogue hash and is rejected on load (tested).
- [x] Every persisted artefact type has a declared version asserted at its load boundary (schema registry).
- [x] Determinism preserved; fixtures re-derived via the real seal path (authenticity verified by re-derivation).

## Verification
Local + CI green: fmt/clippy/test/deny/firewall. 5 new tests in `crates/vintage/src/schema.rs` pass (both AC scenarios non-vacuous — seal a mutated identity via real `Vintage::seal`, expect `Err` from `repo.load`). Reviewer re-derived fixture authenticity: the `backtest_job` golden test loads the committed v3 `sample_vintage.json` through `Vintage::load` (hash-verify) THEN `assert_schema` — passing proves the hash is real and the pinned catalogue equals `current()`. `train_is_deterministic_for_a_fixed_seed` still passes.

## Review verdict — [Approved]
Format 2→3; `CatalogueIdentity` (pure SHA-256 over `\n`-joined ordered indicator ids + `CATALOGUE_VERSION` + `num_states`) persisted in `VintageContent`. `assert_schema` wired into the shared `VintageRepository::load` (`crates/vintage/src/lib.rs:214-216`), fails closed with typed `VintageError::SchemaMismatch`/`GenomeRepMismatch`; `list()` stays tolerant (hash-verify only) so the server read path still enumerates but refuses to load-for-backtest/live. Fixture diffs clean (only format_version, additive `catalogue`, recomputed `content_hash`; genome/lineage untouched; cli+server fixtures + `golden_result.json` consistent). Firewall green — `qe-vintage` deps = `qe-signal` only, no LMDB edge; `sha2` on `qe-signal` is an existing workspace dep (edge-only). AC-3 met: vintage/catalogue/genome-rep asserted here, market/synthetic store at its long-standing `MarketStore::open` → `check_or_init_schema` boundary; the `schema.rs` registry enumerates all four.

Non-blocking nit (recorded, not actioned): `MARKET_STORE_SCHEMA_VERSION = 1` in `schema.rs` is a hand-mirrored copy of `qe_storage::SCHEMA_VERSION` with no compile-time link and no drift-guarding test (adding one would need a `qe-storage` dep). If `qe-storage` bumps its schema this mirror can drift silently — consider a cross-check test in `qe-storage` or a doc-comment link.
