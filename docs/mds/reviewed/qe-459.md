# QE-459 — SPA steering controls with guardrail chips — Review record

- **Ticket:** QE-459 · **Branch:** `qe-459/spa-steering-controls` · **PR:** https://github.com/aoimasu/quant-engine/pull/172
- **Merged commit (branch head):** `be2681c`
- **Base:** `main`
- **Design/evidence note:** [`docs/architecture/qe-459-steering-controls-design.md`](../../architecture/qe-459-steering-controls-design.md)
- **Ticket detail:** [`docs/mds/tickets/QE-459.md`](../tickets/QE-459.md)
- **Design ref:** `docs/architecture/qe-455-research-flow-design.md` §6; QE-450 §13.4

### Green-gate (all on commit `be2681c`)
Frontend (web/ touched): `npm ci` PASS · `npm run lint` PASS · `npm run build` PASS · `npm test` PASS (89 tests)
Rust (insurance): `cargo fmt --all --check` PASS · `clippy -D warnings` PASS · `cargo build --workspace` PASS · `cargo test --workspace` PASS · firewall PASS · `cargo deny check` PASS

### Acceptance criteria — all met
- [x] Form exposes indicator picker (catalogue toggle-chips), search-budget (generations/population), window/fold controls; submits exactly QE-458's whitelisted `TrainParams` fields (`indicator_subset`/`generations`/`population`/`windows`/`folds`/`holdout`/`embargo`), `indicator_subset` omitted for the full catalogue.
- [x] Surfaces the growing distinct-trial `N` (projected, indicative — mirrors `effective_trials_with_features`) rising with subset/budget; archive coverage honestly framed as recorded-after-run (surfaced by the Vintage Inspector). "Pre/post coverage" met to the extent honestly possible (fabricating a pre-run figure would be dishonest).
- [x] Every blocklisted threshold + evolved-pool inclusion renders as a DISABLED guardrail chip; no enabled control can set them (tests assert absence-of-enabled-control, not just chip presence).
- [x] Component tests cover indicator picker, coverage/N feedback, disabled guardrail chips, client floors, server-400 inline.

### Review verdict — Approved (reviewer, commit `be2681c`)

Meets all AC; no blocking issues. Verified against QE-459.md AC, design §6 / QE-450 §13.4, the QE-458 server contract,
the mirrored `evolve/NewCampaign`, the diff, and an independent test run (`NewTraining.test.tsx` 8/8 green).

1. **No bypass affordance — CONFIRMED.** Blocklisted thresholds render as `<button disabled aria-disabled>` chips; evolved-pool is a disabled "not yet supported" chip; `submit()` sets only whitelisted fields. The test asserts absence-of-enabled-control (`queryByRole('textbox'|'spinbutton'|'checkbox', {name:/cost-stress|turnover|capacity|dsr|pbo|ic-fdr/})` all null) + POST body has `evolved_pool`/`evolved_formulas`/`cost_stress_multiplier`/`dsr_cutoff` undefined.
2. **Fields match server contract EXACTLY — CONFIRMED** against `crates/run-protocol/src/lib.rs::TrainParams`; `indicator_subset` sent only for a strict subset (both cases tested).
3. **Coverage/N question — ACCEPTABLE, not blocking.** Pre-run archive coverage is a runtime output with no truthful pre-submit source; fabricating one would be dishonest. Projected `N` = `45·gens·windows·|subset|` faithfully mirrors QE-458's formula, rises monotonically, labelled "Indicative, not the sealed basis" — delivers the teaching intent. Coverage framed as recorded-after-run. Real pre-run figure needs subset-keyed run history that doesn't exist until QE-462 — correctly deferred.
4. **Client hints only remove affordances; server authoritative — CONFIRMED.** Ergonomic floors block the POST early; real server `400` surfaced inline; projected `N` indicative-only.
5. **Tests mirror NewCampaign** — indicator picker, coverage/N, disabled chips, floor block, server-400 inline. Rust insurance gate green (web-only diff).

### Non-blocking notes (repo-precedent, not required for merge)
1. `CATALOGUE_INDICATORS` is a hard-coded client mirror (no `/api/indicators` endpoint) — documented and fail-closed (server 400s unknown ids, so a stale mirror can only over-reject). Matches `NewCampaign` `WINDOW_LATTICE`/`CAP_*` precedent. A future `/api/indicators` endpoint would remove the drift risk.
2. `DESCRIPTOR_CELLS = 45` in the projected-N formula is a client mirror of the server's `DESCRIPTOR_SPACE_CELLS`; drift is purely cosmetic (indicative-only; server authoritative for the sealed N).
