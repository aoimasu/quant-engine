# QE-430 — Deflate the ensemble correlation penalty by sample size — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/132 (squash-merged)
- **Branch**: qe-430/ensemble-correlation-deflation
- **Implementation commit**: 4c2ff32
- **Spec of record**: `docs/reviews/2026-07-16-maxdama-panel-review.md#qe-430`
- **Evidence note**: `docs/architecture/qe-430-ensemble-correlation-deflation-design.md`

## Acceptance criteria
- [x] Property test: over randomised independent member series at small N, the DE search cannot lower the penalty by selecting a sub-threshold sample correlation.
- [x] Case test: a genuinely correlated pair whose fold sample `r` lands below `R(N)` is floored/shrunk to ≈0, while a supra-threshold correlation is still penalised.
- [x] The effective N is recorded alongside the correlation penalty in the ensemble score record.
- [x] Goldens unmoved (train ensemble scoring is not golden-pinned); determinism harness + full suite green.

## Implementation
`crates/ensemble/src/objective.rs` gains a `CorrDeflation` config enum: `SignificanceFloor { z }` (zero any pair with `|r| < R(N)=tanh(z/√(N−3))`, default `z=1.96`) and `FisherShrinkage { lambda }`, defaulted **ON**; `CorrDeflation::None` reproduces raw Pearson behind the toggle. New `pairwise_corr_penalty → CorrPenalty { value, effective_n }` mirrors `TailRisk.tail_n`. `cross_val_score` slices the pool before the objective, so the penalty deflates on the **actual fold-slice N**; effective-N threaded to `SearchResult.corr_effective_n` → `TrainResultDoc.ensemble_corr_effective_n` (and through the regime path).

## Verification (LOCAL green gate re-verified by reviewer on `4c2ff32` — CI/branch-protection disabled for this run)
`cargo fmt --all --check` · `cargo clippy --workspace --all-targets --locked -- -D warnings` · `cargo clippy --workspace --all-targets --all-features -- -D warnings` · `cargo test --workspace --locked` (**769 passed, 2 ignored**) · `cargo deny check` · `cargo test -p qe-architecture --test firewall` (1 passed). Golden `content_hash f59b27cf…` unchanged.

## Review verdict — [Approved] (0 blocking, 2 non-blocking)
1. **Statistics correct.** `R(N)=tanh(z/√(N−3))` (`min_significant_r`, with `n≤3⇒1.0` guard; hand-check `min_significant_r(8,1.96)≈0.7047`) and Fisher-z (`z=atanh(r)`, `shrunk=z.signum()·max(0,|z|−λ/√(N−3))`, `r'=tanh(shrunk)`, NaN-safe) match the spec verbatim. The significance floor genuinely removes the noise-selection path: sub-threshold `|r|<R(N)⇒0`, all negatives→0, so `deflated ≤ raw` always ⇒ the DE minimiser has no sub-threshold gradient to game.
2. **Actual fold-slice N, not full window.** `cross_val_score` slices `s[lo..hi]` before `objective→pairwise_corr_penalty`; the reviewer diffed the three slicing bodies (`cross_val_score`, `regime_aware_cv_score`, and the effective-N re-derivation) and confirmed identical logic, so the recorded effective-N is coherent with what the search deflated on.
3. **Tests non-vacuous.** AC1 runs 600 independent N=12 pairs, asserts `deflated ≤ raw` and every sub-threshold pair contributes exactly the independence baseline 0, with both branches proven to fire (>400 sub-threshold, >0 supra-threshold ~5% leak). AC2 exercises a floored genuinely-positive sub-threshold pair and a still-penalised supra-threshold pair, plus a Fisher case.
4. **Firewall / scope / determinism clean.** No `qe-wfo→qe-ensemble` edge (no Cargo.toml change; firewall names that exact edge and passes). Diff scoped to 7 files; no golden JSON touched; `content_hash` unchanged.

### Non-blocking follow-ups (accepted, deferred to avoid a re-review cycle)
1. `CorrPenalty.effective_n` doc says "admitted pair" but the code mins over **all** pairs (more conservative "tiny-sample" flag — behaviour fine); reword to "any pair" on the next touch of `objective.rs` (e.g. QE-438).
2. AC1 is a pairwise-property encoding rather than an end-to-end DE `search_portfolio` assertion — faithful and non-vacuous; an all-independent `search_portfolio` equality check would close the loop directly. Optional.
