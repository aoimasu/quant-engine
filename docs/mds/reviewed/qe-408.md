# QE-408 — Backtests list filters to backtest runs (client filter + backend `?type=`) — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/115 (squash-merged)
- **Branch**: qe-408/filter-backtests-list
- **Implementation commit**: 030fd5b
- **Spec of record**: `docs/reviews/2026-07-15-team-improvement-review.md` → `### QE-408`
- **Evidence note**: `docs/architecture/qe-408-filter-backtests-list-design.md`
- **Discriminator**: `'backtest'`/`'train'` — FE `BacktestRunMeta.type`; BE `RunSpec::run_type()` → `IndexEntry.run_type`/`RunMeta.type` (QE-406 union, two-variant).

## Acceptance criteria
- [x] A mixed `listRuns` response renders only backtest rows (`BacktestsList.test.tsx`, non-vacuous).
- [x] No training run is reachable via the Backtests table (train row not renderable/clickable/routable).

## Verification
Local + CI green. Rust: fmt/clippy(-D warnings)/test(all)/deny/firewall; `qe-server --test runs` 12/12 (incl. `list_runs_filters_by_type_query`). Frontend: `npm run lint`/`build` (tsc -b && vite build)/`test` (vitest 41/41, BacktestsList 4/4). frontend.yml (build & test, all-features) green on the PR though non-required. No golden touched; no new deps.

## Review verdict — [Approved]
AC client filter (core) is correct and the test is genuinely non-vacuous. `BacktestsList` now `listRuns('backtest')` (over-fetch guard) and `.filter(isBacktestRun)` (client-side defense-in-depth), so state is `BacktestRunMeta[]` and columns read `params.vintage` directly. `BacktestsList.test.tsx` proves the CLIENT filter: `mockRuns` matches on `new URL(url).pathname === '/api/runs'` — query-agnostic — so it returns the SAME mixed payload (one backtest, one train) regardless of the `?type=backtest` the list sends, meaning the server filter is not exercised. It asserts the train row absent (id, window, resolution gone), exactly one `tbody tr`, and clicking the sole row calls `onOpen('bktst-11-aaaa')` and never the train id. Remove the `isBacktestRun` filter and the test fails — non-vacuous — establishing AC-2 (no training run renderable/clickable/routable → no path to a 409/404 detail screen).

Backend `?type=` contract is correct. `list_runs` takes `Query<ListRunsQuery>` (`#[serde(rename = "type")] run_type: Option<String>`); the filter is applied at the INDEX level inside the single QE-411 `spawn_blocking` closure — `if type_filter.is_some_and(|want| entry.run_type != want) { continue; }` — so a non-matching entry is skipped before its `meta.json` is read (no over-read, no per-run blocking). Absent ⇒ all runs, byte-identical to the pre-QE-408 path; unknown value ⇒ empty `200`, not an error. `list_runs_filters_by_type_query` covers none→2/backtest→1/train→1/bogus→0 non-vacuously, and the QE-411 batched-blocking + QE-407 lifecycle tests still pass. Discriminator exact and drift-free across `RunSpec::run_type()`/`IndexEntry.run_type`/`RunMeta.type`/create-dispatch and the QE-406 FE union (two-variant enum → no third type). Scope/parity: `listRuns`'s optional arg leaves `TrainingList` (`listRuns()`) unaffected; server no-filter path byte-identical; blast radius confined to 2 FE files + FE test + `api.rs` + server test; firewall green; no golden/Cargo touched.

- nit (non-blocking): the PR block listed `+ runs/model.rs` among changed files, but `model.rs` was not in the diff — the `run_type`/discriminator infra pre-existed (QE-406-era). Stale changed-files note only, no code impact.
- nit (non-blocking): `frontend.yml` (build & test) is not a required check — only Rust `ci.yml` gates merge. This PR's FE surface is real product code whose correctness rides on the non-gating `frontend.yml` + review; worth promoting the frontend build/test to a required check for FE-touching PRs. → CI-config follow-up (QE-404 theme).
