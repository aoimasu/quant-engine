# QE-461 — Flow supervision: concurrency lane + resume/checkpoint/halt — Review record

- **Ticket:** QE-461 · **Branch:** `qe-461/flow-supervision` · **PR:** https://github.com/aoimasu/quant-engine/pull/174
- **Merged commit (branch head):** `eb548e0`
- **Base:** `main`
- **Evidence note:** [`docs/operations/qe-461-flow-supervision-analysis.md`](../../operations/qe-461-flow-supervision-analysis.md)
- **Ticket detail:** [`docs/mds/tickets/QE-461.md`](../tickets/QE-461.md)
- **Design ref:** `docs/architecture/qe-455-research-flow-design.md` §5.3; QE-407

### Green-gate (all on commit `eb548e0`)
- `cargo fmt --all --check` — PASS · `clippy -D warnings` — PASS · `cargo test --workspace` — PASS (0 failed)
- firewall — PASS (1/1) · `cargo deny check` — PASS · backend-only (no web/)
- NO 5th `RunStatus` variant (enum 0-diff, exhaustive-match test guards it); NO gate/seal/`evaluate_g1` change; NO PROTOCOL_VERSION/VINTAGE_FORMAT_VERSION bump; all changes confined to `qe-server`.

### Acceptance criteria — all met
- [x] Flows run under `QE_SERVER_MAX_FLOW_CONCURRENCY` lane (`flow_permits`, default 1, mirror of evolve); over-limit flow queues; interactive backtests NOT gated by the flow semaphore; a flow past its deadline is terminally marked via the existing `abort → kill_on_drop → terminally-mark` pattern.
- [x] Resume predicate: flow ∧ `meta.train.vintage` present ∧ `train/result.json` exists ∧ `backtest/result.json` absent ⇒ ResumeBacktest (re-spawn ONLY `flow_backtest_phase`, no re-search, deterministic replay from the persisted vintage + train handoff); every other state ⇒ Fail (terminally marked, never re-searched).
- [x] `POST /api/runs/{id}/halt` → terminal `Failed` + `HALT_REASON` in `RunMeta.error` (no new `RunStatus` variant), partial vintage retained; no new status leaks to the gate/vintage path.

### Review verdict — Approved (reviewer, commit `eb548e0`)

Meets all AC; no blocking issues. Verified against QE-461.md AC, design §5.3, the evolve/QE-407 patterns it mirrors,
the diff, and independent test runs (server flow 19/19, runs 23/23, orphan 6/6, halt 2/2, semaphore 2/2, firewall 1/1).

1. **No 5th `RunStatus` variant — CONFIRMED (hard invariant).** `model.rs` 0-diff; halt = `Failed` + `HALT_REASON` (reuses run-type-agnostic `finish_failed`); resume rides `Running`. `flow_supervision_adds_no_run_status_variant` is a genuine exhaustive 4-arm match.
2. **Resume backtest-ONLY, never re-searches — CONFIRMED (safety-critical).** `classify_orphan` is a correct total function; `resume_orphaned_flows` re-spawns only `flow_backtest_phase`. `flow_resumes_backtest_from_sealed_vintage_checkpoint_without_research` uses a real `train_sentinel` probe asserting `!train_sentinel.exists()`; `reconcile_fails_unsealed_orphan_flow_never_researched` proves `resumed==0` for an unsealed orphan.
3. **Resume determinism — CONFIRMED.** Resumed backtest goes through shared `flow_backtest_phase`, rebuilding `flow_backtest_params` from the persisted train handoff (holdout window + instrument + `gate_taker_fee_bps`) pinned to the recorded sealed vintage content hash — deterministic replay; the QE-460 cost-parity guard rides along.
4. **Concurrency lane + deadline — CONFIRMED.** `flow_permits` acquired ONLY for `RunSpec::Flow` (interactive backtests not gated); single `flow_start` clock, both phases drain remaining budget via the existing timeout/abort pattern.
5. **No status leaks; firewall green — CONFIRMED.** Pure supervision; `crates/gate`/`crates/vintage` untouched; startup wires `reconcile_orphans` (fail dead) THEN `resume_orphaned_flows` (resume resumable) before serving.

**Ruling on the flagged deadline choice: ACCEPTABLE (non-blocking).** Reusing the shared ~24h `run_deadline` satisfies the AC ("terminally marked via the existing pattern") and matches reuse-over-new-mechanism; a distinct flow ceiling is a legitimate small follow-up.

### Non-blocking notes (do not block merge)
1. A queued flow holds a pool permit while blocked on the flow permit — inherited from the evolve lane, negligible at default concurrency 1.
2. A both-phases-complete-but-unmarked crash-window orphan is conservatively marked `Failed` rather than recovered as `Succeeded`.
3. The resumed backtest gets a fresh full deadline clock rather than the original flow's remaining budget (reasonable, noted).
