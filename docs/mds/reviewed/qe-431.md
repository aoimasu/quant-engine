# QE-431 — Calibrate slippage half_spread+impact from venue data (shared by friction & capacity) — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/133 (squash-merged)
- **Branch**: qe-431/slippage-calibration
- **Implementation commit**: f7e75d0
- **Spec of record**: `docs/reviews/2026-07-16-maxdama-panel-review.md#qe-431`
- **Evidence note**: `docs/architecture/qe-431-slippage-calibration-design.md`

## Acceptance criteria
- [x] `friction` and `capacity` both consume the calibration input; a test asserts they agree for identical `(side, qty, mark, spread)`.
- [x] The calibration is content-addressed and rides the vintage lineage; re-running on a pinned input reproduces byte-identical coefficients.
- [x] The hardcoded slippage/impact literals are removed from the selection path; a test proves no magic slippage/impact constant remains.
- [x] Goldens regenerated via real code (never hand-edited); `content_hash` move tracked; determinism harness + full suite green.

## Implementation
New content-addressed `SlippageCalibration` in `qe-risk` (§7.7 estimator: median half-spread off the quote distribution; binned zero-intercept LS slope of signed impact vs notional; median-price reference mark; exact `Decimal`; aggressor sign taken directly — **no Lee-Ready**). Both `SlippageModel::from_calibration` (per-contract) and `CapacityModel::from_calibration` (per-$) derive from it; `default() ≡ from_calibration(default)` on both. Rides the hashed `VintageContent`; `VINTAGE_FORMAT_VERSION` 3→4.

## Verification (LOCAL green gate re-run by reviewer on `f7e75d0`, CI disabled — all PASS)
fmt · clippy locked + all-features `-D warnings` · `cargo test --workspace --locked` (**779 passed, 2 ignored**) · deny · firewall · `slippage_parity` (2) · `qe-risk slippage` (6).

## Review verdict — [Approved] (0 blocking, 2 non-blocking)
1. **Golden-regeneration integrity (CRITICAL) — independently CONFIRMED.** Reviewer re-ran `regenerate_fixtures`; `golden_result.json` + `sample_vintage.json` reproduce **byte-for-byte** (empty `git diff`), regenerated `content_hash = 5af3da01d892…7057` matches the committed value. Field diff is **exactly** `format_version 3→4` + the new `slippage` object after `calibration` — chromosomes/weights/calibration/worst_case_loss/catalogue/lineage byte-unchanged. CLI & server `sample_vintage.json` byte-identical; golden references the same hash. `VINTAGE_FORMAT_VERSION 3→4` justified (new hashed field → old vintages fail closed on load).
2. **One source of truth / no drift — confirmed.** Both sides derive, never author; `DEFAULT_HALF_SPREAD`/`DEFAULT_IMPACT_COEFF` and the inline `Decimal::new(1,4)` removed; grep sweep of the selection-path crates found no residual magic slippage/impact literal. Parity test non-vacuous (a mismatched 3× calibration makes the two disagree).
3. **Content-addressed + reproducible — confirmed.** SHA-256 over canonical JSON with `round_dp(18).normalize()`-quantized Decimals is serialize-idempotent and field-sensitive; the fit is byte-reproducible on pinned input; degenerate inputs fall back to defaults.
4. **Firewall — confirmed.** `qe-risk` depends only on `qe-domain`/`qe-error`; adding it to both `qe-wfo` and `qe-ensemble` creates edges to a shared **upstream leaf**, not a `qe-wfo→qe-ensemble` edge. Reconciliation is CONFIG duplication, not a code edge. Firewall test passes.

### Non-blocking follow-ups (accepted)
1. Friction (per-contract) and capacity (per-$) agree **exactly only at `reference_mark`** — inherent to the two unit systems; the mark-portable shape is **QE-440**'s remit (concave sqrt-in-participation). Test correctly uses `reference_mark`.
2. The standalone `SlippageCalibration::content_hash()` method is currently test-only; production reproducibility rides the embedded field in the `VintageContent` hash. Fine as-is.
