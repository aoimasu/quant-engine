# QE-406 — Single-source the CLI↔server↔SPA run protocol — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/103 (squash-merged)
- **Branch**: qe-406/run-protocol-single-source
- **Implementation commit**: fe9216a
- **Spec of record**: `docs/reviews/2026-07-15-team-improvement-review.md` → `### QE-406`
- **Evidence note**: `docs/architecture/qe-406-run-protocol-single-source-design.md`

## Acceptance criteria
- [x] One `ProgressLine` definition in new leaf crate `qe-run-protocol`; cli + server compile against it (both duplicates deleted, re-exported); firewall green.
- [x] Renaming a progress field fails the golden-wire agreement test; `protocol_version=1` carried on the `done` line, server checks it.
- [x] Accessing a backtest-only field on a narrowed `train` run is a TS compile error (discriminated union).
- [x] Wire format preserved — non-terminal lines byte-identical; server still parses CLI stdout; runs/cli tests green.

## Verification
Local: fmt/clippy/test(680)/firewall + agreement(10) PASS; frontend lint/build/test(40) PASS. CI: 11/11 green (required + frontend + all-features-build + 5 feature-gates). Reviewer re-ran agreement(7)/firewall(1)/dependency_topology(1)/server runs(7) on the branch — all PASS.

## Review verdict — [Approved]
Wire compatibility holds (field-by-field diff of the shared `ProgressLine` vs old cli emit: `Progress`/`Gen`/`Ensemble`/`Gate`/`Error` byte-identical in tag/fields/order; only additive `protocol_version` on `done`, `#[serde(default)]`→0 so legacy lines + server fixtures still parse). The agreement test freezes exact wire per variant on serialize AND deserialize (fails on rename/reorder/tag change). Option<f64> tolerance preserved exactly (non-finite→null regardless of `Some`, frozen by a dedicated test). Both Rust duplicates deleted; cli/server re-export shared types so old import paths resolve. Firewall: `qe-run-protocol` deps = serde/serde_json only (pure leaf), `qe-server → qe-run-protocol` adds no forbidden edge, no rule change. `protocol_version` mismatch = warn-and-continue in `manager.rs::drain_stdout` (correct — rejecting would drop a completed run). SPA `RunMeta` is a real discriminated union (`BacktestRunMeta | TrainRunMeta` + `isTrainRun`); all four consumers narrow via `type`/`isTrainRun` with no casts. `describe()` `-inf → "n/a"` is human-line only; machine JSON unchanged.

Non-blocking nit (recorded, inherent to the spec's "mirrored / kept in lockstep" approach): the SPA union is hand-kept in lockstep with the Rust protocol crate — no automated Rust↔TS agreement check, so a Rust field rename fails the Rust agreement test but not a frontend test. Residual contract risk to watch.
