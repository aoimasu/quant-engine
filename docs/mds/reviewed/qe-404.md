# QE-404 — CI builds/lints/tests feature-gated code (http/arrow) — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/99 (squash-merged)
- **Branch**: qe-404/ci-feature-gate-coverage
- **Implementation commit**: b48910217a70e54132a6e556353b8efce5448147
- **Spec of record**: `docs/reviews/2026-07-15-team-improvement-review.md` → `### QE-404`
- **Evidence note**: `docs/architecture/qe-404-ci-feature-gate-coverage-design.md`

## Acceptance criteria
- [x] A bare `unwrap()` or a warning introduced inside any `#[cfg(feature="http")]` / `arrow` block fails CI.
- [x] `cargo build --workspace --all-features --locked` is green in CI.
- [x] The offline default job remains the fast path (not slowed/removed).
- [x] Feature-on coverage exists for: `http` on qe-venue, qe-ingest, qe-server, qe-cli; `arrow` on qe-ingest.
- [x] `unwrap_used`/`unsafe_code` denies apply to the feature-on build.
- [x] No live-network tests added to CI (feature test legs stay network-free).

## Verification
Local: fmt / clippy(default) / test (671 passed, 2 ignored) / firewall all PASS; feature-on clippy+test for http(venue/ingest/server/cli) + arrow(ingest) all PASS; `cargo build --workspace --all-features --locked` PASS. Remote CI: fmt/clippy/test/deny required checks green on b489102; new `feature-gate` matrix + `all-features-build` jobs added (additive, not yet in the required set).

## Review verdict — [Approved]
The two new jobs correctly close the gap. `feature-gate` runs `cargo clippy -p <crate> --features <feat> --all-targets --locked -- -D warnings` then `cargo test -p <crate> --features <feat> --locked` for exactly the required (crate, feature) pairs — `qe-venue http`, `qe-ingest http`, `qe-ingest arrow`, `qe-server http`, `qe-cli http` — feature names cross-checked against each crate's `Cargo.toml [features]`, all match. All four crates carry `[lints] workspace = true`, so the workspace `unwrap_used="deny"` / `unsafe_code="deny"` genuinely bind under `-D warnings`. `all-features-build` satisfies the build AC. Offline `fmt`/`clippy`/`test`/`deny` untouched (fast path preserved). Toolchain 1.96.0 (pinned SHA), `--locked`, and cache mirror existing jobs; YAML valid; no vacuous job. Feature test legs network-free (URL-string assertions + in-memory fakes). Reviewer confirmed `qe-server http` and `qe-ingest arrow` clippy legs locally (exit 0).

Non-blocking nits (recorded, not actioned):
- Each `feature-gate` leg enables one feature at a time; a defect only surfacing with `qe-ingest` `http`+`arrow` together would build (via `all-features-build`) but not be clippy'd/tested. No such combined `cfg(all(...))` block exists today — purely defensive.
- `all-features-build` only builds (no clippy/test of the all-features combo); matches the AC, per-crate legs cover lint/test.

**Follow-up (optional):** consider adding `feature-gate` + `all-features-build` to the branch-protection required set so they block merges (currently required = fmt/clippy/test/deny).
