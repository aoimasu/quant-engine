# QE-446 — Report strategy-level drawdown + optional ceiling at graduation gate — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/148 (squash-merged)
- **Branch**: qe-446/graduation-drawdown
- **Implementation commit**: 3e67cf6
- **Spec of record**: `docs/reviews/2026-07-16-maxdama-panel-review.md#qe-446` (panel P3/low)
- **Evidence note**: `docs/architecture/qe-446-graduation-drawdown-design.md`

## Acceptance criteria
- [x] Max-drawdown (peak-to-trough) statistic on the per-strategy record; computed correctly (ruin ⇒ 1.0).
- [x] Optional drawdown ceiling on the graduation gate, **default OFF** ⇒ graduation byte-identical; a set ceiling blocks a high-growth/deep-drawdown genome.
- [x] No golden moved (stat on non-hashed record); green gate + firewall green; no `qe-wfo→qe-ensemble` edge.

## Implementation
`fitness.rs`: `max_drawdown(returns)` — worst peak-to-trough as a `[0,1]` magnitude (ruin ⇒ 1.0), computed **locally in qe-wfo** (mirrors ensemble `cdar` construction; NOT imported). `strategy_repo.rs`: `StrategyRecord.max_drawdown` (non-hashed record); `try_record`/`record_survivors` thread the stat + gate. `lifecycle.rs`: `QualityGate.max_drawdown_ceiling: Option<f64>` (default `None`), `with_drawdown_ceiling` opt-in, `persists_with_drawdown = persists AND drawdown_within_ceiling`; `persists` left byte-identical. `train.rs` uses `with_defaults()` (ceiling OFF).

## Verification (LOCAL green gate re-run by reviewer on `3e67cf6`, CI disabled — all PASS)
fmt · clippy locked + all-features `-D warnings` · `cargo test --workspace --locked` (**890 passed, 2 ignored**) · deny · firewall · max_drawdown (1) + drawdown_ceiling (4). No golden moved (regenerate → empty diff).

## Review verdict — [Approved] (0 blocking, 2 non-blocking)
1. **`max_drawdown` correct.** Equity compounds from unit start, running peak, `dd = 1 − equity/peak`, ruin (`equity ≤ 0`) → 1.0. Non-vacuous edges: empty/monotone-up → 0; single −20% → 0.2; up-down-recover → 0.5 (later recovery doesn't shrink max); peak-reset → deeper wins; −100%/<−100% → 1.0.
2. **Ceiling default-OFF ⇒ graduation byte-identical.** `persists` unchanged; `None` → `drawdown_within_ceiling` always true → `persists_with_drawdown == persists` (test asserts equality even at 0.99 dd). Set ceiling (0.30) blocks a would-persist 45%-dd genome; shallow still graduates; exactly-at passes, just-over blocked; below-bar never rescued; NaN never passes. Non-vacuous.
3. **No golden moved** — `StrategyRecord` explicitly non-hashed (not in `VintageContent`, rides the record like G1 rides `TrainResultDoc`); diff = `crates/wfo` + note; regenerate → empty.
4. **Firewall clean** (`max_drawdown` local to qe-wfo, no `qe_ensemble` import, no edge); **QE-436 composition** fine (ceiling OFF → survivor set unchanged → parsimony unaffected; ceiling ON is a pure AND-tightening before parsimony).

### Non-blocking follow-ups (accepted)
1. **`StrategyRepository` has no production caller** — `try_record`/`record_survivors` are only re-exported/tested, so the stat isn't recorded and the ceiling doesn't gate in a live train run today. This is a **PRE-EXISTING** unwired state (the graduation-record store was already unwired before QE-446 — which is why the signature change needed no `train.rs` edit), NOT dormant code introduced by this ticket; the ACs are met at the record/gate library level, and with P3/low priority + default-OFF it's acceptable. **Follow-up:** wire `StrategyRepository` into the train pipeline (a separate subsystem-level piece of work) — then the drawdown stat is surfaced and the ceiling can gate.
2. **`StrategyRecord.max_drawdown` lacks `#[serde(default)]`** — a pre-QE-446 record JSONL (no key) would fail to deserialize. Non-issue today (the record JSONL is ephemeral; no committed golden uses it; the repository isn't wired), but add `#[serde(default)]` (as QE-444's `alpha_loss` did) if old records must ever be read forward.
