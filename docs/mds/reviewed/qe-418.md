# QE-418 — Pre-trade gross cap vs true gross exposure — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/111 (squash-merged)
- **Branch**: qe-418/gross-exposure-cap
- **Implementation commit**: 01fadb5
- **Spec of record**: `docs/reviews/2026-07-15-team-improvement-review.md` → `### QE-418`
- **Evidence note**: `docs/architecture/qe-418-gross-exposure-cap-design.md`

## Acceptance criteria
- [x] A book with `long = short = X` (net 0, gross 2X) breaches a `MaxGross < 2X` cap while passing the net cap (`hedged_book_breaches_gross_cap_while_passing_net_cap`, non-vacuous).
- [x] Single-instrument case (gross == |net|) behaves exactly as before (`single_instrument_gross_equals_net_magnitude`, byte-identical).

## Verification
Local + CI green: fmt/clippy(-D warnings)/test(716)/deny/firewall. Reviewer re-ran qe-runtime(113) + firewall — all pass. No golden touched; no new deps.

## Review verdict — [Approved]
AC correctness (core) holds and is non-vacuous. The governor now computes `gross = target.gross.get().abs()` and checks `MaxGrossExposure` with `if gross > c`, while `MaxNetExposure` (and `MaxNotional`/`MaxLeverage`) stay on `mag = |net|` (`crates/runtime/src/pretrade.rs`). `hedged_book_breaches_gross_cap_while_passing_net_cap` constructs a net-0/gross-2X book, asserts `Reject` with a `MaxGrossExposure` breach (20000 > 15000) AND no `MaxNetExposure` breach (net 0 ≤ 1), then re-checks the same gross under a `MaxGross=25000` cap and asserts no gross breach — so the test proves the cap is measured against true gross, not magnitude, and would fail under the old `mag`-based check. Single-instrument parity (critical) is exact: `TargetPosition::single(n)` sets `gross = |n|`, so `gross == mag` and `if gross > c` is byte-identical to the old `if mag > c` on every single-instrument path; `single_instrument_gross_equals_net_magnitude` confirms the gross cap fires identically for long and short, and all pre-existing pretrade/shadow/transport tests pass after purely mechanical retargeting to `single(...)`. No test encoded the old gross==net conflation faithfully-and-wrongly: for a single instrument `gross == |net|` is genuinely true, so the retargeting preserves correct behaviour rather than hiding the bug (which only manifests for hedged/multi-instrument books).

Panic-free on the deny(unwrap/expect/panic) path: `Notional::new` is infallible (`crates/domain/src/money.rs:152`), `single` sets `gross = Notional::new(|net|)` (≥0), `plan` sets `gross = Notional::new(net.gross()·equity)` (`net.gross() = long+short ≥ |net|`, so ≥ |net|), and the governor's `.get().abs()` adds a sign-defensive read — no unwrap/expect/panic anywhere. Blast radius is compiler-bounded and fully covered: every `TargetPosition` construction sets `gross` correctly — `plan` and the AC-test literal set it explicitly, and the shadow/transport test helpers were retargeted to `single()`; no construction was missed. Determinism holds (pure Decimal arithmetic, no RNG/clock), firewall is green, no new deps, and no golden/fixture is in the diff. The netter→hedger→pretrade boundary is coherent; the governor has no live caller yet (same latent state as the other runtime tickets), so the correction bites once multi-instrument netting produces `gross > net` live — composable and ready.

- nit (non-blocking): the invariant `gross ≥ |notional|` is documented and upheld by both real constructors (`single`, `plan`), but `TargetPosition` has public fields and no checked constructor, so a direct struct literal can set `gross < |notional|` (or a negative gross). The governor's `.abs()` only corrects sign, not under-magnitude, so an inconsistent literal would silently under-report gross. No live bug exists (all production paths are correct); recommend a checked constructor / private fields + accessors, or a `debug_assert!(gross ≥ |notional|)`. → folds into **QE-428**.
- nit (non-blocking): scope is deliberately narrow — only `MaxGross` moved to gross; `MaxNotional`/`MaxLeverage`/`MaxNet`/margin/liquidation stay on net magnitude. Acceptable here (no live hedged book yet), but a hedged book's true margin is two-sided, so the margin/liquidation caps will under-model real hedged exposure once multi-instrument netting lands. Recommend a follow-up to reassess those caps against gross when netting grows. → **QE-429** theme.
