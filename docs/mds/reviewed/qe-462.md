# QE-462 — SPA single stepped flow page — Review record

- **Ticket:** QE-462 · **Branch:** `qe-462/spa-flow-page` · **PR:** https://github.com/aoimasu/quant-engine/pull/175
- **Merged commit (branch head):** `4ce3259`
- **Base:** `main`
- **Design/evidence note:** [`docs/architecture/qe-462-flow-page-design.md`](../../architecture/qe-462-flow-page-design.md)
- **Ticket detail:** [`docs/mds/tickets/QE-462.md`](../tickets/QE-462.md)
- **Design ref:** `docs/architecture/qe-455-research-flow-design.md` §5, §14 Q6

### Green-gate (all on commit `4ce3259`)
Frontend (web/): `npm run lint` PASS · `npm run build` PASS · `npm test` PASS (97 tests, 24 files; +7 new)
Rust (insurance): `cargo fmt --all --check` PASS · `clippy -D warnings` PASS · `cargo build --workspace` PASS · `cargo test --workspace` PASS · firewall PASS · `cargo deny check` PASS. Diff is web-only.

### Acceptance criteria — all met
- [x] Flow page configures steer + train window + backtest window + holdout in one stepped form (configure→review→launch) and submits a single `type:"flow"` create; holdout/blocklist thresholds are disabled guardrail chips.
- [x] Monitor (`FlowMonitor`) shows one run row with train→backtest per-phase progression (derived from real `meta.flow` + status; `backtest:'skipped'` on failed-before-seal), holdout/regime chips, "not-paper-confirmed" label, and an "Open in Vintage Inspector" deep-link on success.
- [x] Component tests cover the stepped form, the single-`POST` submission shape, and the holdout/regime + not-paper-confirmed rendering.

### Review verdict — Approved (reviewer, commit `4ce3259`)

Meets all AC; no blocking issues. Verified against QE-462.md AC, design §5/§14 Q6, the QE-460 `FlowParams`/`validate_flow`
contract, the reused QE-459/QE-457 pieces, the diff, and independent runs (all 4 suites green: `NewTraining` 8/8,
`VintageInspector` unchanged, `NewFlow` 6/6, `FlowMonitor` 2/2 = 25/25).

1. **Single `type:"flow"` POST matches `FlowParams` EXACTLY — CONFIRMED.** `NewFlow.buildParams` sends `{seed (required), start, end, resolution}` + `steer.applyTo` (whitelisted-only) via the shared `postRun('flow', …)` choke point. `evolved_pool`/`evolved_formulas` + every blocklist knob absent from the client DTO; NO separate backtest window (backtest rides the server-frozen holdout). Test asserts the exact body incl. `backtest_start`/`backtest_end` `toBeUndefined()`.
2. **No bypass affordance — CONFIRMED.** Blocklisted thresholds + holdout floors render via the shared `CompiledFloorsCard` (disabled chips); no enabled control can set them; POST-shape test proves none submitted.
3. **Regression guard on the extraction — CONFIRMED (behavior-preserving).** `steer.ts`/`steerCards.tsx` faithfully preserve QE-459's logic; `NotPaperConfirmedCallout`/`RegimeComposition` cleanly extracted from `VintageInspector.tsx` (single source of truth for Inspector + FlowMonitor). `NewTraining.test.tsx` (8/8) + `VintageInspector.test.tsx` unchanged and still green — QE-459/QE-457 not regressed.
4. **Per-phase monitor honesty — CONFIRMED.** `FlowMonitor.phases()` derives states purely from real `meta.flow` + status; `backtest:'skipped'` on failed-before-seal (mirrors QE-460/461 atomicity). On success loads the sealed vintage + renders regime chips + not-paper-confirmed; deep-link fires with the correct vintage id.
5. **Client hints only remove affordances; server authoritative — CONFIRMED.** `validate_flow` (reuses `validate_train`) remains the enforcement point; server `400` surfaced inline. Rust gate green trivially (web-only diff).
6. Tests genuinely cover the surface.

### Non-blocking note (repo-precedent, carried over from QE-459; improved here)
`CATALOGUE_INDICATORS`/`DESCRIPTOR_CELLS` remain hard-coded client mirrors (fail-closed; drift cosmetic) — but this PR consolidates them into a single `steer.ts` source of truth for both forms, reducing the drift surface. A future `/api/indicators` endpoint would remove it entirely.
