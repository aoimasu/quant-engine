# QE-258 — Frontend scaffold + design-system port (Vite/React, AppShell, Login) — [Approved]

- **PR:** #85 — https://github.com/aoimasu/quant-engine/pull/85 (squash-merged as `b3387cf`)
- **Ticket:** QE-258 (`Phase: PreP3` · `Area: frontend` · `Depends on: QE-256`)
- **Branch:** `qe-258/frontend-scaffold-login` (deleted post-merge)
- **Merged commit subject:** `QE-258: frontend scaffold + design-system port (Vite/React, AppShell, Login)`
- **Reviewed commit:** `e673fc4` (pass 1, [Approved] 0 blocking / 3 nits — reviewer built & ran the frontend)
- **Evidence / design:** `docs/architecture/qe-258-frontend-scaffold-design.md` (+ `web/README.md`)
- **Changed files:** new `web/` Vite+React app (~42 files), `.github/workflows/frontend.yml`, `.gitignore`,
  design note. Branch carried QE-257 archive bookkeeping in `1d5103c`.

### Goal
Stand up the SPA and port the Claude Design system faithfully (decision: `web/` Vite app).

### Acceptance criteria (from backlog)
- [x] The SPA builds; unauthenticated load shows Login; after a mocked session the shell renders with the
  Research nav; the design system matches the Claude Design tokens.

### Implementation summary
- `web/` Vite + React (TypeScript). **DesignSync faithful port** of the real Claude Design "Quant Engine Design
  System" (`projectId 2b443e2a-…`, the spec's id): tokens verbatim (dark-first violet ramp), primitives
  (Button/Icon/Badge/Callout/Card) + AppShell as typed `.tsx`. **Two documented deviations for no-runtime-CDN:**
  fonts self-hosted via `@fontsource/*` (43 bundled woff2/woff); icons via tree-shaken `lucide-react` (kebab-case
  `name` API preserved).
- Login (net-new): brand lockup, "Sign in with Google" = `window.location.assign('/api/auth/login')` (real
  top-level nav, not fetch), allowlist-rejection `Callout`. `fetchMe`→`/api/me` same-origin drives Login/shell.
- AppShell: Research nav (Strategies · Backtests · Market data) active; Trade/Risk present-but-**disabled**; email in footer.
- Vite `outDir=web/dist`; server serves via `QE_SERVER_STATIC_DIR=web/dist`. `node_modules/`+`dist/` gitignored;
  `package-lock.json` committed. New `.github/workflows/frontend.yml` (npm ci→lint→build→test).

### Verification — BOTH gates PASS (commit `e673fc4`; reviewer independently rebuilt)
- Rust: `fmt`/`clippy -D warnings`/`test --workspace` (664 passed, 2 ignored)/`firewall`/`deny` — PASS.
- Frontend: `npm ci`/`npm run build` (222KB JS/gzip 69KB, fonts bundled)/`npm run lint`/`npm test` — PASS (14/14).
- Frontend CI job ran green on the PR ("build & test").

### Review — [Approved] (0 blocking, 3 non-blocking; reviewer built & ran the frontend)
- **CSP-safe CONFIRMED on the built `dist/`:** zero external-origin references (no googleapis/gstatic/unpkg/cdn,
  no `@import url(https)`, no `window.lucide`, no external script/link). The two deviations genuinely remove the CDN.
- Auth flow genuine (real top-level nav to `/api/auth/login`; `/api/me` drives state; tested). Design fidelity
  verbatim; Trade/Risk disabled not hidden. Tests are real RTL assertions. Diff clean (no node_modules/dist).

### CARRIED FOLLOW-UP → QE-259 (wire the rejection redirect)
The allowlist-rejection UI is built + unit-tested but **cannot fire end-to-end**: QE-256's `/api/auth/callback`
returns a **JSON 403** on a non-allowlisted login (no redirect), while the SPA's `detectRejection` reads
`?error=forbidden|rejected|403|not_allowed|unauthorized` from the URL. **QE-259 must amend the QE-256 callback's
allowlist-reject path to `302 → /?error=forbidden`** (updating the QE-256 auth test that currently asserts JSON
403) so a rejected user sees the styled Callout instead of a raw JSON page.

### FOR FINAL REPORT (repo-admin actions, non-blocking)
- Make the frontend CI job (`frontend.yml` "build & test") a **required** status check on `main` (repo setting;
  the orchestrator/agents can't set branch protection).
- (cosmetic) `index.html` favicon uses a dev source path (Vite rewrites at build; harmless).
