# QE-405 — Firewall guard: qe-runtime / qe-vintage ⊬ training crates — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/100 (squash-merged)
- **Branch**: qe-405/firewall-train-live-decoupling
- **Implementation commit**: 826681a
- **Spec of record**: `docs/reviews/2026-07-15-team-improvement-review.md` → `### QE-405`
- **Evidence note**: `docs/architecture/qe-405-firewall-train-live-decoupling-design.md`

## Acceptance criteria
- [x] Adding `qe-wfo` to `qe-runtime`'s deps fails `cargo test --workspace` (guaranteed by the generic, already-proven rule engine — see verdict); clean workspace passes.
- [x] New rules `qe-runtime ⊬ {qe-wfo, qe-ensemble}` and `qe-vintage ⊬ {qe-wfo, qe-ensemble}` wired into `firewall_rules()` (`crates/architecture/src/lib.rs:227-235`).
- [x] Non-vacuity assertion added: `reachable(&graph, "qe-vintage").contains("qe-signal")` (`tests/firewall.rs:47-52`), plus existing `qe-runtime → qe-venue` covers the runtime rule.
- [x] Doc updated: `qe-cli` sole legitimate both-sides linker; firewall is library-level.

## Verification
Local: fmt / clippy(-D warnings) / test (671 passed) / `qe-architecture --test firewall` PASS on 826681a. Reviewer re-ran the firewall test on the branch (PASS). Remote CI: fmt/clippy/test/deny green. Pure guard-hardening — runtime/vintage confirmed clean of wfo/ensemble (direct + transitive), zero crate moves, no new deps.

## Review verdict — [Approved]
Both new rules correctly wired with the exact `FirewallRule { upstream, forbidden: &[&str] }` shape, matching the AC verbatim. Non-vacuity guards bind (a deps-dropping parser bug fails the test rather than passing vacuously). AC-1 adequately guaranteed without mutating the tree: `check_firewall` is a fully generic engine whose rejection behavior is already proven against the real `firewall_rules()` vec by `detects_a_direct_forbidden_edge` / `detects_a_transitive_forbidden_edge`; the two new rules are structurally identical and present in that vec, and `qe-runtime` is a confirmed non-vacuous graph node — so a `qe-runtime → qe-wfo` edge would produce a `Violation` and fail `firewall_holds_in_the_workspace`.

Non-blocking nit (recorded, not actioned): a one-off unit test constructing a synthetic `qe-runtime → qe-wfo` graph and asserting `check_firewall` returns the matching `Violation` would encode AC-1's exact scenario directly — cheap belt-and-suspenders, not necessary given the generic engine is already proven.
