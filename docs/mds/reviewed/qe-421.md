# QE-421 — Adopt the `qe-error` recoverability taxonomy on the runtime order path — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/123 (squash-merged)
- **Branch**: qe-421/classified-taxonomy
- **Implementation commit**: dd23c78
- **Spec of record**: `docs/reviews/2026-07-15-team-improvement-review.md` → `### QE-421`
- **Evidence note**: `docs/architecture/qe-421-classified-taxonomy-design.md`

## Acceptance criteria
- [x] Every error reachable on the order-emission path maps to a `Disposition` (compile-guaranteed via `_assert_classified<T>` bounds + exhaustive non-wildcard matches + per-variant tests).
- [x] A synthetic fatal error drives to `Halt` (`live_loop_routes_every_error_through_disposition`, non-vacuous).

## Verification
Local + CI green on dd23c78: fmt/clippy(-D warnings, unwrap_used deny held)/test(all)/deny/firewall. Reviewer re-ran qe-error 7/7, qe-risk 30/30, qe-runtime 116/116 (incl. AC test + restart_parity + order_port_conformance), qe-venue 34/34, firewall 1/1. Error text unchanged (grep-confirmed); Cargo.lock delta is 2 edge lines wiring existing `qe-error` (no new crate); goldens unaffected.

## Review verdict — [Approved]
1. **AC every-error→Disposition + fatal→Halt.** `ErrorClass::disposition` is the single source of truth (`Transient→Retry`, `Data→Continue`, `Fatal→Halt`); `Classified` the uniform seam. AC test non-vacuous on the REAL emission path: (a) tripped kill → no fill, `orders_submitted()==0`, `last_disposition==Halt`; (b) `FailingAppendSink` → fill still present, `append_failures==1`, `Continue`; (c) disconnected submit → `Err(Disconnected)`, `Retry`. Exhaustiveness COMPILE-guaranteed: `_assert_classified::<T>()` bounds every order-path type (new type without impl → compile fail) + explicit non-`_` matches (new variant → compile fail) + per-variant tests.
2. **Class-mapping soundness (adversarial) — every mapping checked for "should-Halt-but-Retry/Continue", none found:**
   - `KillHalt→Fatal→Halt` — out-of-band kill halts submission deterministically (core safety).
   - `AppendError→Data→Continue` — SAFE: `pump` offers the journal the already-produced reports post-hoc (audit), so a failed append is an audit-record gap, never a dispatch gate and never an order on unsafe state. Halting live trading on an audit-write failure would be a dangerous fragile coupling. Byte-equivalent to the prior `is_err()` counter.
   - `RestError Fatal|Exhausted→Fatal→Halt` — an exhausted retry budget correctly halts rather than trading on the assumption the REST op succeeded. `RateLimited|Transient→Retry`. Correct split.
   - `WsError` all→`Retry`, `UserDataError` all→`Retry` — connectivity faults the registry recovers by reconnect+resubscribe / listen-key renew + position re-snapshot. Safe because trading on the resulting stale state is prevented by the INDEPENDENT staleness watchdog (`KillHalt→Halt`); halting on every transient WS blip would be too fragile for a live venue. (Non-blocking 1.)
   - `TransportError::Disconnected→Transient→Retry` — `submit_target` returns `Err` early while disconnected (nothing emitted); Retry is a record + propagation, not a spin.
   - `BootstrapError Recon|Decode→Halt`, `CutoverError EmptyReplay|Gap→Halt`, `BootStateError MismatchedEquityPaths→Halt`, `RiskError NegativeLeverage|FractionOutOfRange→Halt` — every broken reconstruction/cutover/state invariant and malformed risk cap halts. `BootstrapError::Rest` delegates to the venue REST split.
3. **Routing correctness / behavior-equivalence.** All three seams route through one `route()` (records `last_disposition`, returns the disposition). No error newly swallowed nor newly propagated: `submit_target` keeps the `Err(Disconnected)` propagation; `pump` append keeps count-and-continue; the `submit` path replaces a SILENT `if let Ok(fill)` (which dropped `Err(KillHalt)` invisibly) with an explicit `match` that still applies no fill on kill but now records `Halt` — stricter/observable, not looser. `Retry` doesn't spin (planner re-sends its latest ABSOLUTE idempotent target on reconnect). `restart_parity`/`order_port_conformance` corroborate unchanged emission.
4. **Panic-freedom.** No unwrap/expect/panic added; `route()` total, all matches exhaustive, clippy `unwrap_used=deny` held. Fatal always halts, never panics.
5. **Firewall + scope.** New `qe-runtime→qe-error` / `qe-venue→qe-error` edges (qe-error a foundational leaf, in no forbidden list) — firewall 1/1, no forbidden reachability. Training-side error handling untouched; no `#[error(...)]` text changed (grep-confirmed); Cargo.lock delta 2 lines wiring existing `qe-error`; goldens unaffected.

### Non-blocking (documented follow-ups)
1. Classifying `UserDataError::Snapshot` (the position-truth stream) as merely `Transient` is correct ONLY because an independent staleness/kill watchdog gates trading on stale position state (`KillHalt→Halt`). Not a defect (QE-421 classifies, doesn't change behavior) — but worth an explicit design note that the connectivity-retry disposition leans on the kill gate as the fatal backstop for position-truth staleness.
2. In the `submit` `Err` arm all three dispositions currently no-op (drop the fill). Correct today (`KillHalt` is always `Fatal`, comment flags it), but a hypothetical future NON-fatal submit error classified `Retry`/`Continue` would silently drop the fill rather than retry — so if a retryable submit error is ever added, that arm needs real wiring, not just the exhaustive placeholder.
