# QE-403 — Net-of-cost truth: funding-coverage gate + non-zero size-impact — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/105 (squash-merged)
- **Branch**: qe-403/net-of-cost-truth
- **Implementation commit**: ce1ec09
- **Spec of record**: `docs/reviews/2026-07-15-team-improvement-review.md` → `### QE-403`
- **Evidence note**: `docs/architecture/qe-403-net-of-cost-truth-design.md`

## Acceptance criteria
- [x] A training window with no/sparse funding data hard-fails (`RunError::FundingCoverage`) rather than sealing.
- [x] A high-turnover genome's fitness strictly drops when impact > 0 (tested, identical trade count).
- [x] Realised funding recorded as a fraction of net PnL (`funding_pnl` + `funding_fraction_of_net` on `TrainResultDoc`).
- [x] Determinism preserved; no golden silently changed.

## Verification
Local + CI green: fmt/clippy/test(688)/deny/firewall. Reviewer re-ran the two new tests + `backtest_over_fixture_store_matches_golden` + determinism + config tests on the branch — all pass. Funding gate runs before `split_with_embargo`/search; `expected = floor(span/8h)+1`, default floor 0.90. Impact default ZERO→0.0001 (same order as 1bp half-spread, quadratic size term). `ac1` recompute verified (slippage 0.04, net −0.14). Config drops `Eq` (keeps `PartialEq`, reproducibility via `content_hash`), adds `selection.funding_coverage_min` with `#[serde(default)]` + [0,1] validation.

## Review verdict — [Approved]
Funding gate correct and non-vacuous (no-funding store → exact `FundingCoverage{0%, 90%}` + empty vintage dir; removing the gate would break the test). Impact default sane. Selection-vs-reporting asymmetry (QE-251 reporting backtest pins `impact=0` to preserve `golden_result.json`) judged **acceptable and non-blocking**: QE-403's AC is selection-scoped, the correctness-critical sealed path prices impact + enforces funding, and the reporting pin is documented (not silent). No golden touched.

Non-blocking nits (recorded):
1. Route reported impact through a CLI flag / the QE-128 capacity model so reporting PnL converges with the selection cost model → **tracked as QE-428** (backlog).
2. Coverage metric under-reports for bar resolutions coarser than 8h (daily bar ≤1 stamp vs 3 expected 8h points) — matches the spec's prescribed metric; typical training is intraday. Worth a doc note if coarse-resolution training is ever supported.
