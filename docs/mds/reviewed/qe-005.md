# QE-005 — CI pipeline — Approved review record

- **PR:** [#5](https://github.com/aoimasu/quant-engine/pull/5) (squash-merged → `main` as c48f9e7)
- **Branch:** `qe-005/ci-pipeline` (deleted)
- **Final commit:** ea16d48
- **Evidence:** `docs/architecture/qe-005-ci-pipeline-design.md`

## Acceptance criteria (met)
- [x] A PR with a clippy warning or failing test cannot be merged.
- [x] CI completes deterministically (no flaky-by-design steps).

## Verification (independently re-run + live-checked by reviewer)
- Local: fmt/clippy/test `--locked`/`cargo deny check` all green.
- CI run on `ea16d48`: all four jobs (`fmt`/`clippy`/`test`/`deny`) success.
- Branch protection on `main`: required contexts `[fmt, clippy, test, deny]`, `enforce_admins=true`,
  `strict=false` — a red required check blocks merge (admins included).

## Review outcome
**Approved** (round 2). Round-1 approved; three non-blocking advisories resolved in round 2:
pinned `dtolnay/rust-toolchain` to a commit SHA; documented the `deny` advisories' RustSec-DB
time-dependency; commented the test job's `clippy` component + added `--locked` to clippy.

## Note on branch protection vs. direct pushes
With required checks + `enforce_admins=true`, direct pushes to `main` (e.g. these archive commits)
may be rejected. Archive bookkeeping for subsequent tickets is therefore folded into the next
ticket's PR branch so it passes through CI.
