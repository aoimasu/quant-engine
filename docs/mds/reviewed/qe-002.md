# QE-002 — Configuration system — Approved review record

- **PR:** [#2](https://github.com/aoimasu/quant-engine/pull/2) (squash-merged → `main` as 5ee6fcb)
- **Branch:** `qe-002/config-system` (deleted)
- **Final commit:** aa18893
- **Evidence:** `docs/architecture/qe-002-config-system-design.md`

## Acceptance criteria (met)
- [x] Invalid config fails fast with a field-level message.
- [x] The same config file produces the same config hash across runs/machines.

## Verification (independently re-run by reviewer, commit d556bcf/aa18893)
- `cargo fmt --all --check` clean; `cargo build --workspace` ok; `cargo clippy --workspace
  --all-targets -- -D warnings` clean; `cargo test --workspace` 27 passed; QE-001 topology guard green.
- AC #2 probed: schema is `Vec`/scalar-only; `content_hash` serialises the typed struct so figment
  merge/env order can't reorder hashed bytes — deterministic on the overlay + forced-profile path.

## Review outcome
**Approved** (round 2). All four round-1 findings resolved:
1. Profiles: `load(profile, base_path)` merges base + `<stem>.<profile>.<ext>` overlay + env and
   forces the requested profile; design note matches shipped API.
2. `history.start` validated as ISO `YYYY-MM-DD` (full calendar validation deferred to QE-007).
3. Blank/duplicate instruments and duplicate reconstructed resolutions rejected with field paths.
4. Unused `qe-domain` dep dropped (returns in QE-007/QE-012).
- Post-approval housekeeping: regenerated stale `Cargo.lock` (commit aa18893) so CI `--locked` passes.
