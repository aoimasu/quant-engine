# QE-443 — Optional inverse-vol (EWMA) seed weighting + capacity water-fill — review record

- **PR**: https://github.com/aoimasu/quant-engine/pull/145 (squash-merged)
- **Branch**: qe-443/inverse-vol-seed-weights
- **Implementation commit**: f5c7481
- **Spec of record**: `docs/reviews/2026-07-16-maxdama-panel-review.md#qe-443` (consensus: **SPLIT** — not strictly superior)
- **Evidence note**: `docs/architecture/qe-443-inverse-vol-seed-weights-design.md`

## Acceptance criteria
- [x] Inverse-vol (EWMA, single decay) seed weighting available; higher-vol → lower seed; equal-vol → 1/N; capacity water-fill layers on top.
- [x] Opt-in, **default OFF** (split consensus, no superiority claim; 1/N preserved); OFF byte-identical (no golden moved).
- [x] Deterministic; green gate + firewall green; no `qe-wfo→qe-ensemble` edge.

## Implementation
`capacity.rs`: `ewma_variance`, `inverse_vol_seed` (single λ; higher-vol→lower seed; equal-vol→1/N; degenerate/flat/short→deterministic 1/N fallback), `SeedWeighting { Equal | InverseVol }` (Default=Equal). `qe-config`: `selection.inverse_vol_seed` (default false) + `selection.ewma_decay` (0.94, validated). `qe-cli` threads it → `cap_or_equal`. Deferred (documented): threading `SeedWeighting` into QE-438 DE scoring; BTC-beta neutralization.

## Verification (LOCAL green gate re-run by reviewer on `f5c7481`, CI disabled — all PASS)
fmt · clippy locked + all-features `-D warnings` · `cargo test --workspace --locked` (**871 passed, 2 ignored**) · deny · firewall · inverse-vol (5) + train_job (5). No golden moved (regenerate → empty diff).

## Review verdict — [Approved] (0 blocking, 3 non-blocking)
1. **Default-OFF byte-identical.** `SeedWeighting::Equal` = exact `1/k` literal → `cap_weights` → `hash_stable`, arithmetically identical to main's `cap_or_equal`; `git diff` shows no fixture/golden; regenerate → empty diff; train_job passes.
2. **Inverse-vol correct.** Single-λ EWMA (standard prior-mean recursion, fixed-order deterministic); higher-vol → strictly lower seed (test: `w[0]>4·w[1]` at ~5× vol); equal-vol → exactly 1/N; degenerate/flat/short → deterministic 1/N fallback (no NaN/div-by-zero); water-fill layers on top. Non-vacuous.
3. **QE-438 interaction — acceptable as documented v1 (reviewer's explicit call).** OFF (default): both QE-438 scoring and deploy use equal→cap → QE-438's optimize-X-deploy-Y closure fully preserved. ON: scoring stays equal→cap while deploy uses inverse-vol→cap — a knowingly-deferred gap, honestly documented. Not blocked (off by default, experimental split-consensus toggle, no superiority claim).
4. **Determinism.** OFF exact 1/k; ON f64 seed → 12dp `hash_stable`-rounded sealed weights (QE-440 argument), never the hashed money ledger; within-run determinism test.
5. **Split-consensus honesty** confirmed (docs present 1/N-robustness vs variance-estimation, "not a strict improvement"); **firewall clean** (portfolio-side only, no `qe-wfo` import, no Cargo change).

### Non-blocking follow-ups (accepted)
1. **Before inverse-vol is ever promoted toward default/production, thread `SeedWeighting` into the QE-438 DE membership scoring** so the object scored equals the object deployed (closes the ON-path optimize-X-deploy-Y gap; harmless at default OFF).
2. ON-path cross-platform reproducibility rests on the 12dp `hash_stable` rounding masking f64 sqrt/division drift — not independently golden-verified (no committed golden enables it).
3. Trivial wasted `returns` clone on the OFF path.
